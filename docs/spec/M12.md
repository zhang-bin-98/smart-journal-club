# M12 工作记录

- 日期：2026-09-07；同日复核并纠正验收结论。
- 原工作分支：`milestone/m12`；文档纠正分支：`codex/docs-m11-m12-status`。
- 目标：完成三篇样例的 v0.4 内容质量、兼容、可编辑 PPTX 和实际发布验收。
- 状态：最终验收未完成；原“Gate 通过”结论保持撤回。M11 已于后续开发中完成，本记录尚未补跑 M12 的三篇内容、办公软件和实际发布验收。
- 已有记录提交：`38b53aa`，只修改本记录与执行计划；提交本身不证明或否定是否执行过验收，结论以可核对的实际证据为准。

## 可保留的历史证据

- [M6 工作记录](M6.md)保存了机制、组学、临床/转化三篇 v0.3 真实内容、人工修正与 PowerPoint 检查，以及当时的实际 Pages 验收。这些结论只适用于所述产物与版本，不自动覆盖 v0.4。
- [M9 工作记录](M9.md)保存了单篇组学的真实规划、人工编辑确认、严格 build、来源/裁图核对和导出证据；生成时确认计划的章节、页面、Claim、Source、Figure/Panel 与布局得到保护。该记录同时明确将三篇 v0.4 最终内容和 PowerPoint 检查留给 M12。
- [M10 工作记录](M10.md)及既有固定合同保护 Agent 提问权限、proposal 应用、取消、版本和 Undo；这些有效成果继续复用。
- `tests/migration-contracts.ts` 确有 Current/Previous 迁移及失败保护检查；`tests/pwa.mjs` 确有本地生产构建的离线、资源、更新与多标签保护检查。验收缺口不等于这些实现或历史检查全部失效。
- [M11 工作记录](M11.md)现已补齐工作区、问题定位与统一导出门槛，并保留固定主链、Edge、PWA 和截图验证；这些结果可作为 M12 最终主链的实现基线，但不能代替三篇内容、办公软件或线上发布证据。

## 尚缺的最终验收

| 项目 | 证据缺口与待完成动作 |
|---|---|
| 三篇 v0.4 内容 | 原记录仅引用 M6 三篇旧检查和 M9 单篇组学，未逐篇按当前叙事、大纲、证据与可读性要求给出复核发现。可复用既有产物，但须明确所用版本及其适用结论；只有相关生成逻辑变化或发现问题时才重新生成。 |
| 新稿 PowerPoint/Keynote | M6 组学 PowerPoint 证据对应旧的 13 页稿；M9 新稿为 16 页，记录了浏览器审阅和导出，没有对应的新稿办公软件打开/编辑结果。需核对当前交付 PPTX 的文字可编辑、图像比例和主要几何，不能拿旧稿替代。 |
| 最终主链 | M11 已将实际工作区、Agent proposal、问题定位、统一门槛、旧稿修复及导出失败重试接入同一固定响应主链；M12 仍须在当前待交付文稿上完成内容和办公软件验收，不另建重复端到端矩阵。 |
| 科学内容与图源问题 | M6/M9 曾发现术语/因果边界、部分证据 Panel 缺失、bbox 遗漏轴或图例，以及规划来源和收尾错误，并进行人工修正。原 M12 未逐项说明当前结果已修复、依赖人工调整还是仍有限制。历史问题不能推定全部仍存在，也不能视为已自动消除。 |
| 当前版本实际 Pages | `tests/pwa.mjs` 使用本地 HTTP 服务和项目子路径，证明的是本地生产 PWA；原 M12 未给出相应部署提交、Actions 结果或线上刷新/依赖加载证据。实际发布状态尚未在本轮核实，需在最终验收时单独记录。 |

详细验收标准仍以[执行计划第 3 节](../执行计划.md)为准。复用三篇已有论文和同一 fixture；不要求三篇全部重新调用模型或重复编辑、恢复、离线主链。模型原稿、人工确认计划与人工修正成果必须区分，不能把修正稿的表现当作原稿正确率。

## 检查证据与适用范围

原 M12 记录报告以下命令通过，保留为历史报告；本轮未重跑这些命令：

- `npm run check`：162 项单元测试、lint、格式、类型和生产构建。
- `npm run check:browser`：固定响应的上传/生成、编辑/来源/裁图、Agent proposal、重新分析、恢复与既有导出回归。
- `npm run check:pwa`：本地生产构建的项目子路径、离线重开、IndexedDB、懒加载导出、更新等待及多标签保护。
- `git diff --check`。

复核 `tests/pwa.mjs` 可见，PPTX 自动断言为下载及 ZIP 文件头检查，不等于在 PowerPoint/Keynote 打开并编辑。本地子路径服务也不等于当前 GitHub Pages 已完成线上验收。

本轮以 `git show --stat 38b53aa`、`rg` 和 UTF-8 文件读取核对记录、脚本与历史证据；未重新生成论文、运行应用测试、打开 PPTX 或验证线上部署。未将推测写成实测失败或通过。

本轮文档修订检查已通过：一次性 `node` 只读脚本（经 Windows PowerShell 标准输入执行）验证五份修改文档的 UTF-8、Markdown 代码围栏、46 个本地链接及仓库相对路径；`git diff --check` 通过。仅更新三份主文档与 M11/M12 记录，未改源码、提示词、测试或发布配置；以上结果仅为文档检查。

## Gate 与下一步

M12 Gate 未通过：当前版本的三篇内容、可编辑导出与实际发布证据仍未闭环。M11 的自动检查与界面验收不能代替这些产品验收。

后续按用户授权补上述实际证据，保留模型原稿与人工修正、旧版本与当前版本的区分。科研事实仍以论文原文与人工复核为准，不宣称已被模型或确定性校验自动证明。
