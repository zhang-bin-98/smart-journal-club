# M12 工作记录

- 日期：2026-09-07。
- 工作分支：`milestone/m12`。
- 目标：完成三篇代表论文的 v0.4 内容质量、旧稿兼容、可编辑 PPTX、生产 PWA 与实际 GitHub Pages 验收。
- 状态：已完成，Gate 通过。

## 完成内容

### 三篇当前稿复核

本轮复用 M6 人工修正稿和 M9 组学确认稿，不重新调用模型；先通过当前代码迁移、校验并重新导出，再逐篇检查叙事、证据、图源和 PowerPoint 成品。历史稿只作为输入，以下结论均对应本轮重新生成的 v0.4 产物。

| 论文 | 当前稿 | 内容复核与保留的人工修正 | 当前校验 |
|---|---:|---|---|
| 机制研究 | 14 页、7 章；结果 8 页 | 区分 long/short ModT 半衰期，保留 `dccA–01583` operon 边界、异构体/异源互补限制并收窄因果表述 | 无硬错误；6 条章节转场提醒和 7 条长标题提醒经逐页复核后接受 |
| 临床/转化 | 15 页、7 章；结果 9 页 | 区分 safety 与 PK 人群，使用 GM 而非 GMT；`PT80=200` 仅作为预测阈值，不写成疗效结论；静脉给药潜力仍标为未验证 | 无硬错误；6 条章节转场提醒和 11 条长标题提醒经逐页复核后接受 |
| 组学研究 | 16 页、9 章；结果 8 页 | 保留 Torin1/rapamycin 区分、低谷氨酸与残余自噬解释、`tor2-ts10` 温度条件，以及 Psk1/Rps6 作为 readout 而非已证实中介的边界 | 无硬错误；仅 Take-home 有 1 条长标题提醒，经逐页复核后接受 |

三篇均形成背景 → 问题 → 设计 → 主要结果 → 综合/局限 → Take-home 的完整故事，结果页占主体；结论对应现有 Figure/Claim。上述科学表述来自既有人工修正并由本轮人工复核确认，不宣称模型或确定性校验能够自动证明科研事实。

### 真实问题与修复

本轮发现并修复两个由当前 v0.4 验收暴露的兼容/导出问题，未改变 Schema、产品交互或 Prompt：

1. v1 旧稿的 Figure/Panel 已带合法来源，但没有在页面 `sourceIds` 重复记录同一来源；叙事校验曾因此把机制稿 3 页、临床稿 1 页误报为 `figure-source-mismatch`。`deckNarrativeView()` 现在只在只读校验视图中派生已展示 Figure/Panel 来源，继续保留未知来源和 Plan 严格校验。
2. 页面同时有显式 Citation 和自动来源脚注时，组学稿第 16 页会重复显示同一页来源。预览与 PPTX 导出现在按论文页码去重，显式 Citation 优先，避免不同 Source ID 指向同一页时仍重复。

架构原已规定 Figure/Panel 来源由元素派生且不要求在页面重复，因此本次是实现纠偏，不新增数据契约。相关文件：

- `src/modules/outline/narrativeRules.ts`
- `src/modules/outline/validateNarrative.ts`
- `src/modules/paper/sources.ts`
- `src/modules/deck/export.ts`
- `src/ui/editor/SlidePreview.tsx`
- `tests/unit/narrativeDeckRules.test.ts`
- `tests/unit/sources.test.ts`

## PowerPoint 与视觉验收

三篇均由当前生产模块重新导出，并在 Microsoft PowerPoint 16.0 实际打开。检查脚本通过 PowerPoint 对象模型临时修改第一处原生文本再恢复，未保存文件；导出前后 SHA-256 一致，证明文字可编辑且验收没有改写交付文件。

| 论文 | 幻灯片 | Shape / 原生文字 / 图片 | 实际渲染抽查页 | 结果 |
|---|---:|---:|---|---|
| 机制研究 | 14 | 62 / 52 / 10 | 1、5、9、11、14 | 无越界、图片比例异常或文字溢出候选 |
| 临床/转化 | 15 | 68 / 61 / 7 | 1、5、6、11、15 | 无越界、图片比例异常或文字溢出候选 |
| 组学研究 | 16 | 77 / 58 / 19 | 1、6、10、11、13、16 | 无越界、图片比例异常或文字溢出候选；第 16 页来源去重后复查通过 |

逐页抽查确认正文、图片、图注和来源可读，没有裁切、重叠或拉伸。计算机界面自动化运行时因本机沙箱 helper 不可用而停止重试；本轮改用已审阅的 PowerPoint COM 验收脚本完成实际应用打开、编辑、渲染和关闭，不把 ZIP 文件头或浏览器下载当作办公软件验收。Keynote 与 Chrome 未安装，未记为实测；Gate 由 PowerPoint 和 Edge 覆盖。

## 自动检查与主链

- `npm run check`：通过；Biome lint/format 共 116 个文件，Vitest 16 个文件共 167 项测试通过，TypeScript、5 个研究策略/5 个阶段提示词检查及 Vite 生产构建通过。仅有既存 Zod Rollup 注释警告。
- `npm run check:browser`：第一次因项目未安装 Playwright 运行包而在启动前失败；指定 Codex 已提供的 Playwright 模块后通过。固定响应主链、M11 工作区、检查/导出合同在 Microsoft Edge 中通过，无真实模型调用。
- `npm run check:pwa`：使用同一 Playwright 模块通过 4 组生产 PWA 检查，覆盖安装失败重试、项目子路径、冷启动离线、PDF/裁图/保存/懒加载导出/IndexedDB、旧缓存隔离、等待更新、dirty/busy、多标签和 API 不缓存。
- 三篇当前稿迁移/校验/导出脚本：通过；机制与临床从 v1 明确迁移到 v2，组学保持 v2；三篇均无硬错误、无页面异常或外部请求，未调用模型。
- PowerPoint 16.0 实际打开、文字编辑恢复、几何/图片比例/文字边界和 PNG 渲染抽查：三篇全部通过，文件哈希未变化。

M11 已完成的统一工作区与固定响应主链继续作为实现基线；本轮没有再创建另一套 E2E 矩阵，也没有重复消耗模型额度。

## 实际发布

- 应用提交 `14cf687` 已合并并仅推送 `main`。
- GitHub Actions “Deploy to GitHub Pages” 运行 `34086987835` 于 2026-09-07 成功完成，对应提交 `14cf68791cb1b1cc76cabedc76176a4aef6cfc0f`。
- 在 `https://zhang-bin-98.github.io/smart-journal-club/` 使用 Microsoft Edge 实际验收：入口、`manifest.webmanifest` 和 `sw.js` 均返回 200；线上 service worker 版本 `7e6114a53adc3bdf` 与本地当前构建一致；刷新后仍受当前 service worker 控制；模型设置懒加载成功；无页面错误或失败请求。

线上生产构建不暴露开发专用 `#/fixture` 页面，因此固定 fixture 的完整编辑/导出在本地生产 PWA 与浏览器主链中验收，线上只验证实际入口、当前部署版本、刷新、依赖加载和 PWA 注册；这一区分符合既有发布边界。

## Gate 结论与限制

M12 Gate 通过：三篇当前内容、人工修正边界、旧稿迁移、Current/Previous 保护、可编辑 PowerPoint、图片比例与主要几何、离线/本地存储、更新保护及实际 Pages 发布均已闭环。M0–M12 至此完成。

保留限制：研究事实仍需论文原文与人工复核；本轮未重新调用模型，内容结论适用于已保存的人工修正稿；未安装的 Chrome 和 Keynote 未作为实测环境。用户已有未跟踪文件 `docs/REFACTOR_BRIEF.md` 未修改、未暂存。

## 发布后图页分析修复（2026-09-07）

- 工作分支：`codex/fix-figure-analysis-output`。
- 目标：处理用户在“分析 Figure / Panel”阶段收到无定位的输出校验错误；该阶段早于 `5eec122` 修复的大纲规划。
- 状态：实现及固定响应回归通过；用户这次论文的具体非法字段与真实模型重试结果未验证。截图仅证明图页输出不符合要求，不能据此认定一定是 bbox 缺失或越界。

实际发现：`analyzeFigures()` 原先遇到任意 `ModelOutputError` 就终止，没有携带原图的单页修复；异常消息也没有失败页码。检查 `z.toJSONSchema(BBoxSchema)` 的实际输出确认，原数值字段只有 `type: number`，放在 `superRefine()` 内的范围限制未发送给模型。

完成内容：

- `src/modules/paper/figurePage.ts` 承担单页 Schema、一次修复及错误定位，`analysis.ts` 继续负责完整阶段工作副本和 ID 分配。仅对输出校验失败修复一次，复用原图、页码、文字、图像区域和诊断；网络、认证、额度、截断及取消错误不进入修复。
- 修复仍失败时显示 PDF 页码与固定的问题类别；原始输出和诊断仅保留在异常原因中，不进入可见消息。失败阶段不提交任何部分图源，也不推进 checkpoint。
- `src/shared/schema.ts` 用数值约束明确传递 bbox 范围及坐标解释，保留联合边界校验；合法坐标集合和持久化结构不变，不做坐标猜测或越界裁剪。
- 产品失败反馈与架构中的单页修复约定已同步至权威段落。

实际检查：

- `npm run test -- tests/unit/figureAnalysis.test.ts`：8 项通过，覆盖 wire Schema 范围、越页拒绝、原图与诊断回传、空页、两次失败上限、安全提示、请求错误/取消和工作副本不回写。初次三项失败来自测试初始化钩子误返回 mock，被 Vitest 当作清理函数执行；修正钩子后通过。
- `npm run check`：118 个文件 lint/format、17 个文件共 179 项单测、提示词检查、TypeScript 和生产构建通过。仅有既存 Zod Rollup 注释警告。
- `npm run check:browser`：用脚本现有 `SMARTJC_PLAYWRIGHT_MODULE` 入口指定已提供的 Playwright，Edge 完整主链通过。在同一主链第 8 页注入两次缺少 bbox 的输出，确认只修复一次、显示页码、数据库仍为 `pdf-parsed` 且无部分图源；用户重试后再模拟一次失败及成功修复，核对修复请求含同一原图与诊断，随后继续完整主链。
- `npm run check:pwa`：4 组生产检查通过，覆盖安装、子路径、离线重开、PDF/裁图/导出、更新等待及本地数据保留。
- `git diff --check`：通过。

验证边界：以上均为固定响应，不调用真实模型；未取得用户截图对应的失败响应，尚不能确认该论文的具体坏字段及修复后的实际分析结果。发布后的应用重试可通过页码和原因类别进一步定位。用户原有 `docs/REFACTOR_BRIEF.md` 保留不动。

发布证据：应用提交 `2c416ca8f3a5d584c2610e3b7bd1d754601df02f` 已合并并推送 `main`；[Pages 部署运行 34123768279](https://github.com/zhang-bin-98/smart-journal-club/actions/runs/34123768279) 状态为 `completed/success`。无缓存读取生产入口返回 200，并引用当前构建入口 `assets/index-B9IWNOvG.js`；图页模块所在的 `ProjectPage-DVrUfMCn.js` 返回 200，响应字节 SHA-256 与本地已验证构建一致。此处只证明新代码已发布，不代替用户论文的真实模型验证。

## 发布后长时间等待排查（2026-09-07）

- 工作分支：`codex/audit-generation-stalls`。
- 用户反馈：图源分析等待约半小时，取消后能恢复操作；要求从头检查并运行测试。
- 状态：已修复并验证应用层无界等待与缺少图页进度的问题；同一论文真实图页调用通过，但本次未复现原先的半小时等待，原始请求的具体状态仍未知。

### 排查证据

1. 修改前 `npm run check` 全部通过（179 项测试）。原测试使用快速固定响应，没有覆盖不结束的流迭代器或最终结果 Promise，因此测试通过不能证明慢请求场景正确。
2. 用本地同一份 18 页论文运行应用的 PDF.js 解析、1800 边长渲染与图像区域提取。候选页为 PDF 第 3、4、5、7、8、9 页，合计约 1.6 秒；每页 PNG data URL 约 1.02–1.47 MB，低于单次 8 MB 请求预算。查看首个候选页渲染，Figure、Panel 和图注均可见，没有空白或截断。
3. 候选页循环有限，按页顺序处理，每页至多分析一次及格式修复一次，没有自动网络重试。若每次成功或非法输出都接近 180 秒预算，6 页的累计等待窗口可接近 36 分钟；这只是上界分析，不是对原始半小时等待的根因证明。任意请求超时或请求失败应立即中断该阶段。
4. SDK 0.85.1 的正常流会产生终止事件，但应用原先只把 AbortSignal 交给 SDK，仍直接等待流迭代和 `result()`。注入“无任何事件”“流结束但结果不完成”“取消后底层不结束”时，旧代码都保持 pending；补丁后独立退出等待。这个缺陷由故障注入确认，不能声称线上那次 SDK 一定出现了同样行为。

### 完成内容

- `src/shared/llm/model.ts`：将 SDK 加载、流迭代和最终结果纳入同一个可取消等待边界；继续保留 180 秒预算、零自动网络重试和原有错误分类。超时/取消后不依赖底层先结束，正常结束移除监听；迟到流内容不再进入 UI。
- `src/modules/paper/analysis.ts`、`figurePage.ts` 与 `src/modules/generation/runGeneration.ts`：显示原 PDF 页码、已完成/总候选页数和准备、分析、仅一次修复状态；异步结果返回后再次检查取消。进度只在内存，不改变 Paper Schema 或完整阶段原子提交规则。
- `tests/unit/model.test.ts`：补充 SDK 不结束、最终结果不完成、取消/迟到内容与正常流清理测试；`figureAnalysis.test.ts` 增加六页有限修复、进度顺序和图像准备后取消检查。
- `tests/figure-stalls.mjs` 接入原浏览器主链，只增加这篇报错论文的阶段检查，不复制编辑/导出流程。缺少用户本地样例时明确 SKIP；本轮样例存在且实际执行通过。修复数、请求页序和阶段保存均有断言。
- 产品文档与架构中的进度、取消和请求预算权威段落同步更新。未改变模型、提示词、部署配置或逐阶段保存行为。

### 实际验证

- `npm run check`：120 个文件 lint/format、18 个文件共 185 项单测、提示词检查、TypeScript 和生产构建通过；仅有既存 Zod Rollup 注释警告。测试编写中曾因 Node 联网环境模拟、夹具页码不连续和 SDK 类型导入失败，修正测试后通过，未放宽应用校验。
- `npm run check:browser`：Edge 完整主链通过；报错论文取消和提前触发超时后均退出 busy，数据库保持 `pdf-parsed`、18 页、零部分 Figure。重试对六页分别注入一次非法输出与成功修复，严格产生页序 3、3、4、4、5、5、7、7、8、8、9、9，然后只进入一次理解并停在 `paper-ready`。
- 单独以 390 × 844 视口调用同一 `checkFigureStalls()`：取消、超时、进度和恢复均通过，长进度文案不妨碍取消操作。
- `npm run check:pwa`：4 组生产检查全部通过，覆盖安装重试、子路径、离线重开、PDF/裁图/导出、更新等待与数据保留。
- 格式化测试辅助代码后重跑 `npm run lint`、`npm run format:check`、`git diff --check`：通过。

### 经授权的真实图页调用

最初真实调用被权限审查阻止，未执行。用户先明确同意发送 PDF 第 3 页，随后明确同意发送剩余第 4、5、7、8、9 页至现有 DeepSeek API。使用现有本地配置和实际 `requestFigurePage()` 路径，Key 不进入代码、日志、记录或部署产物；只记录页码、耗时、图数、Panel 数及安全诊断类别，没有保存原始模型响应。

| PDF 页 | 返回 Figure | Panel 数 | 近似耗时 |
| --- | --- | ---: | ---: |
| 3 | Fig. 1 | 5 | 9.8 秒（含启动及解析） |
| 4 | Fig. 2 | 5 | 9.9 秒 |
| 5 | Fig. 3 | 8 | 8.8 秒 |
| 7 | Fig. 4 | 6 | 8.2 秒 |
| 8 | Fig. 5 | 6 | 7.7 秒 |
| 9 | Fig. 6 | 10 | 11.6 秒 |

六页均一次返回并通过当前 Schema 校验，无格式修复、超时或失败，总耗时约一分钟。这里验证的是单页真实请求及其结构，不代表已完成整篇真实论文理解、大纲和幻灯片生成，也不代表图框与科学内容已逐项人工确认；后续阶段没有真实调用。原先半小时等待的请求日志不可用，本轮没有复现，不能认定是死循环或已证明其唯一根因。后续若再次出现等待，新进度可直接确定页码和当前子状态。

用户原有 `docs/REFACTOR_BRIEF.md` 未修改、未暂存。

发布证据：应用提交 `1db5b6b7a15be5fd8ab07c5ab6837fa452756a43` 已快进合并并仅推送 `main`；[Pages 部署运行 34134364175](https://github.com/zhang-bin-98/smart-journal-club/actions/runs/34134364175) 为 `completed/success`。无缓存读取线上入口确认引用 `assets/index-DKBBl-t9.js`；入口脚本、`assets/ProjectPage-Cu6Nu6lx.js` 和 `sw.js` 均返回 200，响应字节 SHA-256 与本地已验证生产构建一致。线上已有会话仍须结束当前任务并通过“更新并刷新”采用新版本；不能仅凭部署成功认定旧页面已自动换用新代码。

## 发布后大纲问题定位与修改引导（2026-09-07）

- 工作分支：`codex/outline-issue-guidance`。
- 用户反馈：问题提示无法对应到具体部分，也不知道如何修改大纲。
- 状态：实现、单元、完整浏览器主链、生产 PWA 与实际 Pages 发布核验通过。

### 完成内容

- `src/modules/outline/ui/OutlineIssues.tsx` 与 `issueGuidance.ts`：将编辑区下方的重复裸提示改为上方问题卡，分别统计硬错误与提醒；给出页码/标题、章节、具体 Figure/Panel/原文页码或 Claim 正文及结果页。图源校验结果增加临时 `panelId` 定位，不改变持久化 Schema 或校验门槛。
- `OutlineEditor.tsx` 与 `ProjectPage.tsx`：点击“定位修改”选择对象、滚动并聚焦目标字段或图/结论，高亮当前项并提供具体说明。全局缺项明确显示为全局；同一问题重复点击仍能定位，保存后已经解决的问题不再残留说明。页面帮助区分章节编辑与带页码的页面编辑。
- `OutlineSlideForm.tsx`：保存/取消移动到固定顶部工具栏；图源登记缺项可显式补全当前页面已选有效图源的来源，操作先留在输入草稿，核对保存后才写入。取消、保存、Undo/Redo 和刷新沿用 OutlineSession；未知 Panel 不降级成整图，不改论文或现有选图。
- 研究问题缺项可显式在首个结果章节前添加零预算空章，有符合位置的现有研究问题章时复用入口；该章新增页默认为研究问题。用户自己填写内容、页数预算及过渡，不自动编造论文内容或调整已有页面。重点结论提醒说明增加实际结果页、改为简略讲或核对后接受的选择，不强迫添加空页。
- 产品文档第 3.5 节与架构“计划修改与确认”同步交互与提交边界；没有修改模型、提示词、生成流程、依赖或发布配置。

### 实际验证

- `npm run check`：124 个文件 lint/format、19 个文件共 191 项单元测试、提示词检查、TypeScript 与生产构建通过；仅有既存 Zod Rollup 注释警告。
- `tests/unit/outlineGuidance.test.ts`：6 项新增固定 fixture 测试，覆盖不同 Panel 的问题键/定位、Claim 实际结果页及未分配提示、全局问题与插入点、字段选择、来源补全的保存与撤销、未知 Panel 不降级及引用去重。
- `npm run check:browser`：Edge 完整主链通过。`tests/outline-guidance.mjs` 接入同一浏览器脚本并复用叙事 fixture，在独立测试项目注入缺研究问题、三处图源来源不匹配及重点页数不足；实际操作定位、重复定位、输入未保存保护、补全/取消/保存、问题减少、Undo/Redo、刷新恢复、Claim 修改、新增研究问题章与页、正文及预算字段定位，最后无硬错误并显式接受结果占比提醒后确认，刷新保留确认状态。
- 该浏览器用例同时以 390 × 844 视口核对无横向溢出，检查桌面问题列表与移动端定位截图；移动端具体 Panel 高亮、固定顶部保存/取消/图源补全和当前问题说明可见。
- `npm run check:pwa`：4 组生产检查通过，覆盖安装失败重试、Pages 子路径、冷启动离线、PDF/裁图/保存/导出/IndexedDB、旧缓存隔离、等待更新、dirty/busy、多标签和 API 不缓存。
- 测试编写期间修正了刷新后等待渲染和新增一页后仍有结果占比提醒的断言；没有为让用例通过而放宽校验或把提醒改成错误。
- `git diff --check`：通过。

验证边界：本轮不调用真实模型，不重复发送论文，不修改用户线上浏览器里已保存的大纲，也不宣称模型原始规划内容已自动修好。用户采用新版本后仍需按具体提示核对并保存；红色错误未解决不能确认，橙色提醒可以核对后接受。已有未跟踪文件 `docs/REFACTOR_BRIEF.md` 保留不动。

发布证据：应用提交 `e3f3de1ee164dc2b2cbda01f564230f54ef5a80c` 已快进合并并仅推送 `main`；[Pages 部署运行 34140229671](https://github.com/zhang-bin-98/smart-journal-club/actions/runs/34140229671) 为 `completed/success`。无缓存请求生产入口返回 200，确认引用 `assets/index-siP4CYcM.js`；该入口脚本、`assets/ProjectPage-DyDtBQQz.js`、`assets/index-CKSj0bvq.css` 和 `sw.js` 均返回 200，响应字节 SHA-256 与本地已验证构建一致。用户已有会话需先保存或取消待保存输入，再通过“更新并刷新”采用新版本；不需要重新分析或清空项目。
