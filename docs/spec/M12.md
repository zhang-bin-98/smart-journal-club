# M12 工作记录

- 日期：2026-09-07
- 工作分支：`milestone/m12`
- 状态：已完成，Gate 通过。

## 最终验收

- 复用 M6 已保存的机制、组学、临床/转化三篇代表论文人工检查与 PowerPoint 检查记录；本轮不重复调用模型，保留模型原稿与人工修正的区分。
- M9 组学样例在本版本重新完成规划、人工确认、严格 build、来源核对、裁图与导出；确认计划的章节、页面、Claim、Source、Figure/Panel 顺序均保持。
- M10 Agent 提案和 M11 检查与导出页已接入同一 DeckSession、版本与导出门槛。

## 实际检查

- `npm run check`：通过，162 项单元测试、lint、格式、类型和生产构建。
- `npm run check:browser`：通过，包含上传主链、三阶段保存、编辑/来源/裁图、Agent 提案、重新分析、恢复与导出回归。
- `npm run check:pwa`：通过，包含 Pages 子路径、冷启动离线重开、IndexedDB、懒加载导出、更新等待及多标签保护。
- `git diff --check`：通过。

## Gate

M9–M11 的 v0.4 主链、三篇既有人工内容检查、PPTX 导出和 PWA/Pages 静态发布检查均有证据。科研事实仍以论文原文和人工复核为准，模型输出不被视为自动证明。
