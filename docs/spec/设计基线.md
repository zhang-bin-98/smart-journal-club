# 设计基线工作记录

- 日期：2026-09-05
- 状态：设计文档与工作流程已完成，静态验收通过。
- 工作分支：设计
- 合并目标：main
- 范围：产品、架构、执行计划和仓库工作流程；M0–M6 均未开始。

## 完成内容

- 将原有设计资料收敛为三份权威设计文档，保留 Git 历史中的原始内容。
- 恢复首页、项目准备、生成、编辑器、来源/裁图及设置的页面设计和交互状态。
- 约定 Markdown 正文与 YAML 头信息的研究叙事策略，明确策略选择、阶段保存和更新边界。
- 建立 AGENTS.md，明确精简实现、必要验证及独立改动自动提交。
- 本次增加 main 主分支、里程碑独立分支、完成后写入 docs/spec 并合并回 main 的流程；同步执行计划和架构中的相关引用。
- 统一文档与交付中的相对路径约定，清除工作记录中的本机路径。
- 全文复核后补齐：阶段提交的项目/检查点核对、未保存草稿与旧 AI 响应的处理、导出快照、删图后的布局回退、整套重生成输入入口，以及提示词配置在构建时的校验。
- 用户确认允许删空文稿：保留新增页及撤销、禁用零页导出；首次自动生成仍要求至少一页。复用已有布局、事务和 fixture，不增加应用代码、测试框架或额外模型复审。

产品与运行规格见 [产品文档](../产品文档.md)、[架构文档](../架构文档.md)；阶段状态见 [执行计划](../执行计划.md)，协作方式见 [AGENTS.md](../../AGENTS.md)。

## 验证

环境：Windows / PowerShell，检查在仓库根目录执行。以下记录检查命令，省略仅用于本机执行的环境参数。

- 一次性 PowerShell 静态核对通过：5 份 Markdown、18 个可解析的本地链接，7 个既有页面线框逐一比对无变化，编码及代码块格式正常。
- `rg` 路径扫描和 `git -c core.autocrlf=false diff --check` 均通过；文档使用相对路径，没有错误记录目录残留。
- `git merge-base --is-ancestor main HEAD` 通过；本轮在已同步 main 的设计工作分支完成。
- 检查范围包括本地链接、Markdown 格式、绝对路径残留、零页状态、草稿处理、阶段事务、导出快照和执行阶段的一致性。
- 候选资料核对：DeepSeek 的预定模型 ID 与图像输入有[官方说明](https://api-docs.deepseek.com/guides/vision/)；GLM 候选的精确 ID 和视觉能力仍待 M0 核对。本次没有真实模型调用，不能视为 CORS 或主链通过。

结论：已补齐本次发现的文档契约缺口，文档静态验收通过。应用代码、模型、PPTX 与 Pages 验证均未执行，不代表 M0–M6 通过。

## 未解决事项与下一步

- M0–M6 的 Gate 均未验证，设计文档完成不等于实现验收通过。
- 当前未配置 GitHub 远程或发布流程；模型候选和浏览器能力留待 M0 实测。
- 下一阶段为 M0，开始工作前从 main 创建 milestone/m0；本次不创建应用代码或提前开始该阶段。

## 已有关键提交

- `7de7507`：精简产品与架构文档，统一 PWA 执行计划。
- `9023e9e`：恢复页面设计，完善可配置研究叙事策略。
- `0390173`：建立工作约定，明确协作流程与自动提交规则。

本次流程修订及最终合并节点以 Git 历史为准，不预填尚未产生的提交号。
