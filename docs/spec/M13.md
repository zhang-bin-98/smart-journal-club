# M13 v0.5 设计契约统一

- 日期：2026-09-08。
- 工作分支：milestone/m13。
- 基线：main 与 origin/main 同步，起点为 53e38ea。
- 目标：按用户确认方案完善三份权威文档和工作约定，仅交付文档。
- 状态：文档完成，已继续补充四层架构与工作台 UI 重设计；静态 Gate 结果见下文，v0.5 应用功能尚未实施，M14–M18 未开始。

## 完成内容

- [产品文档](../产品文档.md)：以全文底稿和图证据为基础，Figure 切分核对为唯一默认人工暂停点，核对后自动完成大纲/演讲稿、规划与 PPT；每步允许主动介入。默认充分覆盖发现并标出主线/补充，移除固定默认页数与双重预算维护。
- 产品同时补齐图源核对工作台：整图/Panel 拖框、缩放、补删、标签修正、Undo/Redo、自动基线恢复、候选重识别；明确核对重点是切分，不是要求用户逐条审查科学字段。
- [架构文档](../架构文档.md)：更新论文全文块与原句定位、Claim–Evidence–Source 多对多查询、切分确认版本及人工结果保护、大纲/讲稿/页面分配、自动 ready 状态、暂停/恢复、计划构建合同及有限修复。
- 架构区分可编辑工作 Paper 与文稿绑定的冻结 Paper，保护 Current/Previous；源级修改不静默改变旧稿。讲稿与最终 Deck 一起保存，PPTX 备注由唯一讲稿内容派生；旧稿缺讲稿不编造或锁住。
- [执行计划](../执行计划.md)：增加仅文档的 M13 及未实施的 M14–M18，分阶段覆盖底稿、切分、自动生成、工作台和验收；扩展同一 fixture/主链/三篇样例的必要验证，不新建评分平台或组合矩阵。
- [AGENTS.md](../../AGENTS.md)：同步文档阶段边界与新工作流，明确旧 M0–M12 阶段约定不能阻止已确认的新设计。
- 图源核对、大纲/讲稿、幻灯片、检查四个视图统一为固定窗口外壳、内部独立滚动；保留原有设置、草稿/输入法、来源、单页裁图、AI proposal、Undo/Redo、零页、版本、离线和导出异常处理。

## 已确认方向与本轮契约细化

用户确认：核对切分后自动生成、每阶段可介入；自动切分不好时可手动调整；默认充分覆盖发现并区分主线与补充；讲稿参与规划并决定 PPT 内容；科学标注完整优先于无重叠，共享图例可被不同 Panel 适度重复包含；本轮先更新文档。

为落实这些方向，本轮在架构中细化了 TextBlock/原句区间、讲述片段与页的分配、程序 ready 与图源核对确认的区别、论文版本冻结与有界回收、迁移和局部失效等契约。这些是待实施的设计，不宣称用户逐字段指定过接口，也不当作当前运行代码。

切分思路参考 [split-multipanel-figure](https://github.com/Genesi599/split-multipanel-figure) 的标签锚点、内容块和间隙分析，保留模型语义识别、局部图像与人工修正。没有直接移植算法，不把固定阈值、自动标签识别或 L 形规则写成已验证有效；不引入 Python/SciPy 服务、OCR 引擎或遮罩输出。

## 实际检查

环境：Windows / PowerShell，使用仓库现有 Node.js 与 TypeScript 进行一次性文档静态检查。沙箱 helper 初始化失败后，通过获准的命令执行方式完成读写和检查；没有应用运行阻塞需要规避。

- git status --short、git branch --show-current、git remote -v、git log -5 --oneline：核对基线与用户已有未跟踪文件。
- git fetch origin、git rev-list --left-right --count main...origin/main：开始时 0/0；按仓库规则创建 milestone/m13。
- 分段阅读三份原文、相关现有 Schema/UI 与引用任务，核对历史事实和当前实现边界；没有读取或吸收未跟踪任务书为既定设计。
- rg -n 与完整差异审阅：核对强制大纲确认、固定默认页数、预算差额、Notes 候选、只允许单页裁图等有效旧冲突已替换；历史段落显式隔离。
- 一次性 node --input-type=module 脚本（通过标准输入，不写入仓库）：UTF-8、Markdown 代码块、本地相对链接、M13–M18 编号、变更白名单检查通过；架构四段 TypeScript 定义在内存合并检查，零诊断。
- 相同脚本对比 HEAD 的执行计划：M0–M12 历史阶段定义正文保持不变；docs/spec/ 的原有历史记录未修改。
- git diff --check：通过。

首次文档提交前加入本记录与最终状态后运行静态检查：5 份文档、48 个本地链接、15 个代码块通过检查，其中 4 段 TypeScript 定义零诊断，历史阶段正文保持不变。提交结果以 Git 历史为准，不预填提交号或合并结果。

## 同阶段补充：四层架构与 UI 重设计

- 日期：2026-09-08；继续 milestone/m13，补充起点为已合并的 a0d2ec9。用户在架构讨论后明确要求先完善文档，并重新设计使用体验较差的 UI；本轮不启动实现。
- 架构第 1 节用四层依赖表、三个领域所有权、目标目录与实际文件归位表替换旧职责图。Plan/Deck 同属 presentation，复用共同定义/纯规则但保留各自会话；workflow、任务与跨对象提交由 app 统一协调，PDF/模型/存储/PPTX 为可替换适配器。
- 补齐成果/任务/UI 状态归属、事件转换和修改影响表；明确 UI/tool 不控制事务、候选不覆盖旧稿、提取完整性与汇报覆盖分开。
- 产品第 3 节重设计工作台：两行导航与单一阶段主操作、对象导航和上下文工具、Panel 框选/键盘微调/实际裁图、自动保存与失败保留、讲述/页面双向定位、AI 输入按需展开、检查定位、窄窗口与焦点。线框、尺寸与交互为设计细化，未冒充用户逐像素指定或已完成可用性验证。
- 执行计划将迁移职责分配到 M14–M17，M18 核对完整目标；分层测试与真实 UI 任务各有验收，旧 M0–M12 定义及工作记录不改写。AGENTS 同步新边界，未新增架构/产品的第二份权威文档。
- 使用 domain-modeling 技能梳理术语/边界，按仓库约定集中在架构文档；没有另建 CONTEXT、ADR、原型网页或实现空壳。

### 本次实际验证

开始时默认 SSH 22 端口连接超时；第一次 443 连接因主机名对应的密钥记录不匹配而停止，没有关闭主机密钥验证。随后以命令级 core.sshCommand 使用 443、Hostname=ssh.github.com、HostKeyAlias=github.com，复用已信任的主机密钥，git fetch origin 成功；main 与 origin/main 为 0/0，复用同阶段分支。未更改远端或持久化 SSH 配置。

实际执行 git diff --check 与一次性 node --input-type=module 静态脚本，检查通过：5 份文档、48 个本地链接、16 个代码块，架构 4 段 TypeScript 定义在内存合并后零诊断。脚本同时检查新分层/UI 约定、变更白名单和 M0–M12 历史阶段正文不变；原历史工作记录无改动。审阅产品/架构/执行计划及 AGENTS、阶段记录差异，修正了重复主操作示意与 Agent 适配器边界。仅检查文档；没有运行应用 build、unit、browser、PWA 或模型调用，不据此宣称新层次或新 UI 已通过运行验收。用户已有 docs/REFACTOR_BRIEF.md 未读取、未改动、未暂存。

### 本次提交与推送阻塞

设计修订提交 376a70e 已通过文档检查并快进合并本地 main。自动审批两次拒绝推送：认为具体 GitHub 远端的归属未得到可信用户确认，向该目的地传输数据授权不足。只读核对显示 origin 为既有发布仓库 https://github.com/zhang-bin-98/smart-journal-club，远端 main 仍为 a0d2ec9；待推送差异仅五份文档，未包含未跟踪任务书。补充证据后仍被拒绝，因此停止推送并等待用户明确确认目的地，不改变远端、不绕过审批。文档静态 Gate 通过与远端推送未完成分别记录。

## 同阶段补充：交互性能约束

- 日期：2026-09-08；继续 milestone/m13，起点为 4753579。用户要求补充页面卡顿相关文档，随后明确第 5 项“性能单独验收”暂不考虑。
- 架构第 3 节纳入前四项：拖框/输入轻量路径，业务像素计算的 Worker 适配与取消，按实际内容局部更新，分级位图/同页复用/容量与并发管理。保留完整提交、Undo、旧稿证据与导出质量，不以删页、降科研图清晰度或跳过校验优化。
- 产品第 3.1 节补充加载、快速切换与迟到结果、输入和任务响应的用户行为；执行计划将实际职责接入 M15–M17，但没有新增专项性能测试、时延/帧率指标、压力 fixture 或硬件矩阵。
- 依据前一轮代码只读检查：已有 PDF.js Worker、有限缓存与释放，拖框不逐次重绘 PDF；裁图编码、缩略图挂载、提交复制与打包为风险位置。未进行性能录制，不能认定当前卡顿根因。引用已读取的 MDN Worker 与 Canvas 编码资料作为机制依据，不代表本项目性能已验证。
- 本轮 git fetch origin 使用此前已验证的命令级 SSH 连接，读取结果为本地 main 领先 2 条、远端无新提交；复用同阶段分支，保留用户未跟踪任务书。之前具体远端授权的自动审批阻塞仍在，用户本轮未确认目的地，因此不重试推送或更改连接配置。
- 实际执行 git diff --check 与一次性 node --input-type=module 文档静态脚本：四份改动文档及 AGENTS 共五份检查通过，48 个本地链接、16 个代码块、4 段架构 TypeScript 定义零诊断；M0–M12 历史阶段正文保持不变，变更白名单与暂缓专项性能验收条款检查通过。完成本轮差异审阅；没有修改应用、Prompt、依赖、测试或发布配置，没有性能测试与真实模型调用。

## 同阶段补充：确认原型归档与文档结构重构

- 日期：2026-09-08；继续 milestone/m13，起点 d05b8ed。用户确认图源核对页面可行，要求将 HTML 收入文档并重构结构；随后明确所有部分拆成文件夹，并增加接口文档和数据模型约束。本轮范围仍为设计资产，不启动 M14–M18。
- docs/README.md 为唯一总入口；product、ui、architecture、execution 分别拆分正文，prototypes 保存可操作设计稿，spec 继续保存原工作记录。旧三份长文仅保留跳转兼容，各目录给出原章节对应；历史阶段移入 execution/历史阶段.md，仅修正相对链接，M0–M12 实际记录不改写。
- docs/prototypes/figure-review.html 原样归档用户确认的最终稿，SHA-256 为 f2d0023aae3098e52fbb1ecdc50704cd225406cae60aab327605cf2c65c6118e。原型使用示意数据及本地演示状态，不参与生产构建/部署，不据此宣称真实分析或一键 PPT 已实现。原型与应用契约的差异在 ui/图源核对.md 明确列出。
- UI 规格同步连续滚动 Figure、一次整体确认、Panel 图注/公共说明默认展开、正文证据折叠、完整图注高亮和手动关联。桌面优先，移除新版本响应式/自动抽屉要求；左导航 176px，余下默认 5∶4，右侧分隔线可拖动、记忆和复位。严格裁切选区，避免侧栏变宽时露出邻图。
- architecture/数据模型.md 集中四段类型定义；数据约束.md 保存原不变量并补全图注关联约束。新增 CaptionAssociation 和 Figure 的完整图注来源字段为目标设计，区分专属/共享说明、自动/人工、已建立/待复核；修改几何不丢关联，标签变更保留并复核，手动集合受保护。原文仍只存 TextBlock，关联及反向查询不复制事实。
- architecture/接口契约.md 明确浏览器内查询/命令、输入输出、前提与失败语义、事务和取消边界、PDF/Worker/模型/Pi/存储/PPTX 适配职责。没有新增后端、通用框架、实际接口空壳或第二套 Schema。使用 domain-modeling 技能核对术语与关系，按用户指定结构维护，不另建重复 CONTEXT/ADR。
- 实际检查：git fetch origin 后本地 main 领先 3 条、远端无新提交；复用原分支。一次性 Node 检查 UTF-8、全部本地链接、代码围栏、历史段落与记录保真、HTML 哈希和脚本语法；四段数据模型类型合并后 strict/noEmit 零诊断。首次内存类型检查遇 Windows 路径匹配问题，修正检查器后通过，不属于模型定义错误。
- 实际在桌面 Edge 的 1249×974 窗口从仓库归档路径打开 HTML，检查图注选择/手动关联与撤销、侧栏拖动及裁切矩形、固定导航、连续 Figure 定位和生成演示返回；页面脚本错误为零。此前原稿的裁切留白像素检查已证明无选区外图形，归档哈希完全一致，不重复性能或模型测试。文档差异与必要引用人工复核，git diff --check 通过。最终静态检查：46 份 Markdown、165 个本地链接、21 个代码块、4 段模型类型通过，历史原文与归档 HTML 一致性检查通过。
- 未执行应用 build/unit/browser/PWA 或真实模型调用，未修改 src、prompts、tests、依赖、工作流。已有 docs/REFACTOR_BRIEF.md 未读取、修改或暂存。当前原型可用不表示其他页面已获逐页确认。
- 原有推送自动审批阻塞仍在：具体远端归属与目的地授权此前被拒绝，本轮用户未确认目的地，不重试推送。继续完成不受阻的本地文档验证、提交和合并；阻塞与文档 Gate 分别记录。

## 同阶段补充：大纲讲稿原型与分步生成

- 日期：2026-09-09；继续 milestone/m13，起点 57d9679。用户确认将页面规划移到幻灯片、暂缓跨步骤一键生成，明确一段讲稿可对应多个 Panel、同一 Panel 可对应多段讲稿。本轮更新设计文档及 HTML，不启动应用实现。
- docs/prototypes/outline-speech.html 归档当前讨论稿：跨章节连续编辑、目录拖动、主线/补充标签独立排序、多选图源、Panel 反向讲述入口、拆合段落、撤销与保存。去除页码/页数/页面规划、重复上下文行和后台 PPT 模拟任务；下一步只展示交接说明，第三步仍待设计。
- 两页导航统一居中并增加间距；figure-review.html 保留已确认的修框、图注、裁切和侧栏交互，只同步导航与“确认切分并生成讲稿”流程，演示到讲稿停止。此前记录的原始归档哈希属于历史基线，本次文件已按授权更新。
- 产品与 UI 定义分步推进；架构移除目标 Project.pauseAfter，区分用户下一步授权与程序 ready。讲稿来源已有 sourceIds 承载多对多，反向查询派生，取消单处关联不影响他处。幻灯片承接分页/合页和跨图组图；有限图组参数与控件在下一页设计时继续细化，不以此宣称完整幻灯片规格或实现已完成。
- 实际检查：git fetch origin 只读同步成功，本地 main 领先四条，远端无新提交；复用原工作分支。两份 HTML 的内联脚本通过 Node vm.Script 语法检查，使用已有 Tailwind 编译为无外部依赖的单文件，不安装依赖。
- 实际使用本地 Edge 1325×974 验证：多图选择、同 Panel 跨讲述定位和独立取消、拆分/合并及撤销、补充章节前移而标签不变、改标签而顺序不变、文本/顺序刷新保留、步骤入口与图源阶段停止；页面脚本错误为零。两侧导航留白相等，项目外层不滚动，截图人工检查通过。旧 Demo 的段落内页面字段仅从该演示状态剥离，保留讲稿与引用；正式应用版本保护规则不变。
- 补查旧 Demo 编辑保存发现原 onfocusin/onfocusout 属性未触发预期委托事件，改为显式 addEventListener；通过失焦立即保存、v1 内容读入并写回 v2、刷新保留、跨章节输入、拆合撤销及侧栏调宽验证。补丁首次语法检查发现换行匹配问题，未写入文件，修正后语法与浏览器复测通过。
- 文档静态检查最终通过：45 份 Markdown、163 个本地链接、21 个代码块、4 段目标模型 strict/noEmit 零诊断，历史记录未改、两份原型副本一致，git diff --check 通过。首次类型检查器意外加载无关 @types，限制为文档自身与标准库后通过，未改应用依赖。
- 不修改 src、prompts、tests、依赖或发布配置，不调用真实模型，不运行应用构建；浏览器检查针对原型，不能证明实际生成能力。旧推送审批阻塞仍未解除，本轮不重试推送。

- 2026-09-09 用户进一步确认大纲与讲稿页面没有问题，要求更新文件并进入下一页讨论。本次起点 b2247ea，确认状态同步到 UI/原型索引、页面规格、执行状态及 HTML 标识；原型交互和模型契约保持不变。幻灯片进入讨论，尚未制作原型，具体布局与控件不记作已确认。
- 本次确认登记仅修改状态文字与索引；本地链接、HTML 副本一致性、脚本语法及 git diff --check 通过，不重复浏览器/模型测试。远端只读同步无新提交，已有推送审批阻塞未解除，不重试推送。

## 同阶段补充：幻灯片连续工作台讨论稿

- 日期：2026-09-09；继续 milestone/m13，起点 a95760e。用户确认中栏滚动切换幻灯片、底部讲稿/右栏跟随当前页，并提供专注模式。本轮制作 HTML 用于下一页讨论，不启动应用实现。
- docs/prototypes/slides.html 提供固定七页、讲稿跨页/多段同页、Panel 独立复用、分页/合页、讲述移动、删页后重新安排、图组多选/排序/列宽/间距、文字编辑、撤销及本地保存；大纲页下一步打开该示例。三页状态独立，未调用模型或生成 PPT，不把演示数据当作真实版本化应用内容。
- UI 规格记录已确认的滚动/专注方向，具体控件、尺寸和图组版式仍待确认；页序与章节身份分开，排序不自动改变主线/补充。图组与分页的完整运行参数尚未作为应用 Schema 实现。
- 本地 Edge 在 1440×974 检查初始画布截图，1325×974 检查连续/专注、Panel 多选/顺序/大小/间距、按句换页、合页保留内容、讲述移页、讲稿编辑与撤销、删页/删空及重新安排、保存刷新、两条分隔线、附图提前而身份不变。脚本错误为零；原型没有外层纵向滚动。
- 首次自动化 dragTo 对长目录取到错误源节点；用实际指针按下、拖动及边缘自动滚动验证后通过。补齐上移/下移替代；分页前后片段集合不丢失不重复，删除只解除分配。没有扩大模型/论文/性能测试矩阵。
- 最终静态检查通过：45 份 Markdown、170 个本地链接、代码围栏、两份原型的脚本语法与归档副本一致性，git diff --check 通过。归档路径的大纲下一步导航和连续/专注切换经浏览器复核，零脚本错误；未改应用、Prompt、依赖或发布配置。
- 两次大型保存脚本的自动审批超时，文件编辑工具另遇沙箱 helper 初始化错误；确认未执行后，改用同一审批路径的直接 Copy-Item 与明确文本编辑，归档成功。此前远端推送目的地审批阻塞不变，不重试推送。

## 同阶段补充：幻灯片 AI 输入与修改预览

- 日期：2026-09-09；继续 milestone/m13，起点 334487e。用户指出幻灯片原型缺少 AI 输入，本次按已有公共交互约定补齐底部可展开输入和右栏 AI 对话，不启动应用实现。
- docs/prototypes/slides.html 支持当前页/所选页面/整稿范围、提问/修改模式、发送/取消、修改前后预览、应用/放弃及一次撤销。发送时固定目标，浏览不扩大范围；手工编辑取消待处理请求并使旧提案失效。新回复定位到差异，右栏无第二输入框。已应用编辑使用原有本地保存，对话和待应用提案不跨刷新恢复。
- 使用明确标注的固定规则演示排列、间距与屏幕文字精简，提问仅返回范围摘要；未知修改明确提示未执行。同步页面规格与原型索引，没有接入真实模型，也不宣称自由改写讲稿或分页已经实现。
- 本地 Edge 1325×974 验证通过：默认折叠/展开、应用前页面不变、切换页面后目标仍固定、应用与整次撤销、取消后无迟到修改、手工编辑后提案过期、提问只读、未选页面禁用发送、多页修改与单次撤销。新回复定位可见，外层无纵向滚动，页面脚本错误为零。
- Node 内联脚本语法、原型副本一致性、Markdown 本地链接/代码围栏及 git diff --check 通过。仅修改设计资产，未运行应用构建/测试或真实模型。远端只读同步无新增提交；既有推送目的地审批阻塞未解除，不重试推送。

## 同阶段补充：幻灯片确认与检查导出讨论稿

- 日期：2026-09-09；继续 milestone/m13，起点 e2513b8。用户确认幻灯片页面可行，要求更新文件并进入下一页。同步 UI/原型索引、页面规格、执行状态和 HTML 确认标识；检查与导出仍为待讨论稿，不把新控件记录为已确认。
- docs/prototypes/check-export.html 新增桌面三栏：问题分类与列表、页面/图证据定位、原因/影响/修复；右栏可调宽，保留底部 AI 入口。修复候选预览、应用并复检、撤销后检查过期、内容建议不阻断、覆盖与用户省略原因、原文关联缺口分别展示。
- 导出模拟固定快照、进度、取消与失败重试，提供仅建议/无问题/过期/失败/零页场景。幻灯片检查入口打开该页；四页状态独立，不检查前一页实际编辑，不生成 PPTX，AI 仅固定解释，不调用模型。样例的用户省略记录为合成情境，不是替当前用户删减论文内容。
- 本地 Edge 1325×974 检查通过：初始阻断、图注/正文切换、两项修复与预览前不修改、剩余普通建议时导出可用、撤销后必须复检、覆盖/省略/原文缺口、AI 示例、过期复检、导出取消后无迟到完成、失败重试、零页与无问题状态。页面脚本错误为零，截图核对分栏与可读性，无外层横向或纵向滚动。
- 静态检查通过：45 份 Markdown、174 个本地链接、代码围栏、两份受影响原型的脚本语法与副本一致性，git diff --check 通过。补查步骤导航、分隔线键盘调节/双击复位、刷新保留示例状态通过。
- 复用现有 Tailwind 和浏览器运行时，未安装依赖或新增应用测试；本轮只修改设计资产，不运行应用 build/unit/PWA 或真实模型。只读 fetch 后本地 main 领先八条、远端无新提交；保留原推送审批阻塞，不重试推送。

## 同阶段补充：检查与导出并入幻灯片

- 日期：2026-09-09；继续 milestone/m13，起点 20c916d。用户确认取消独立第四步，检查作为幻灯片内功能，覆盖主要回归大纲；最终导出校验保留。本轮同步产品、UI、架构入口、执行约定和 AGENTS，未启动应用实施。
- 三份有效 HTML 导航改为论文与图源、大纲与演讲稿、幻灯片。幻灯片顶栏提供检查提示与直接导出，右栏展示问题并定位同一画布，页目录和画布标记提示；编辑、AI 应用与撤销后从现有状态派生，不维护独立问题状态。旧 check-export.html 仅跳转，原设计保留于 Git 历史。
- 普通建议无需逐项确认；示例可通过底部图源故障入口检查阻断、恢复、撤销。讲述待安排复用既有分配入口，来源编辑与 AI 继续使用原用例。导出按钮自动核对当前示例，失败定位问题，成功过程固定页面/讲稿快照，支持取消；不生成 PPTX，不执行真实几何或科研检查。
- Edge 1325×974 验证三步导航、问题定位同一画布、普通建议直接导出、图源未就绪阻断/恢复/撤销、取消后无迟到完成、旧 URL 自动跳转通过，零页面脚本错误。截图复核分栏、主操作与问题提示可读；设计行为不等于真实应用能力。
- 静态检查通过：45 份 Markdown、170 个本地链接、代码围栏、三份原型内联脚本语法及四份兼容副本一致性，git diff --check 通过。补查 AI 修改、零页禁用导出、撤销恢复和无外层滚动通过。
- 只读 fetch 后远端无新提交，复用同阶段分支；本轮未修改应用、依赖、Prompt 或历史工作记录，未调用模型/运行应用构建。原远端推送目的地审批阻塞未解除，不重试推送。

## 同阶段补充：检查标签与必须修复的导出门槛

- 日期：2026-09-09；继续 milestone/m13，起点 33c714e。用户要求检查成为右栏并列标签，删除顶栏检查按钮；主动弃图和覆盖变化只提醒，页面放不下等问题必须处理后才能下载。
- 幻灯片原型新增检查 Tab，复用右栏内容、筛选与画布定位；必须修复项数量在标签显示。底部阻断演示可选择必需图源未就绪或模拟页面溢出；后者预览候选后应用，并可撤销/重做。导出校验不因用户浏览过问题而通过。
- 产品/UI/数据约束明确：移除页面 Panel 不删除论文原图，不自动等于省略 Claim；人工删减的覆盖不足不阻断、不强制补图或补写理由。当前稿仍需使用的资源缺失、无效引用及内容无法容纳继续阻断；模型自动漏提取不能记为用户选择。
- Edge 1325×974 验证顶部无重复按钮、五个右栏标签、检查选中/ARIA 关联、页面溢出拦截/预览前不修改/修复/撤销/重做、主动删图后导出、图源拦截与恢复通过，零脚本错误；截图核对标签和侧栏可读。模拟溢出只切换示例状态，尚未实现导出几何与排版算法，不生成 PPTX。
- 静态检查通过：45 份 Markdown、170 个本地链接、代码围栏、原型脚本语法、副本一致性和 git diff --check。
- 仅修改原型与相关设计文件；只读 fetch 无远端新增提交，未改应用/依赖/测试，未调用模型或运行应用构建。既有远端目的地审批阻塞保留，不重试推送。

## 同阶段补充：三页交互确认完成，进入项目管理讨论

- 日期：2026-09-09；继续 milestone/m13，起点 51b8618。用户确认当前几页没有问题，要求更新文件并确认项目准备/项目管理页。
- 同步 UI 目录、幻灯片规格、原型索引与执行状态：本轮确认涵盖 Figure 核对、大纲讲稿、幻灯片及右栏检查 Tab、强制处理技术阻断和主动弃图仅提醒。项目准备文档标明当前进入讨论，原文字基线仍保留，新布局/表单承载尚未确认。
- 文档静态检查通过：45 份 Markdown、170 个本地链接、代码围栏与 git diff --check；相关差异已复核。
- 本轮仅登记设计确认和下一讨论范围，未改 HTML 交互、应用、数据 Schema 或依赖，未制作未经讨论的新项目页；不重复浏览器/应用/模型测试。只读 fetch 无远端新增提交，原推送目的地审批阻塞未解除，不重试推送。

## 同阶段补充：项目管理原型与主论文/补充 PDF

- 日期：2026-09-09；继续 milestone/m13，起点 04a2c6c。用户确认项目准备成为新建流程的一部分，支持一份主论文 PDF 和一份可选补充 PDF，授权更新文档与制作网页原型。
- docs/prototypes/projects.html 提供项目列表、搜索/阶段筛选、打开/重命名/删除确认、空状态、双文件选择/拖放、新建名称与要求、稍后开始、模型未配置提示和可取消的模拟分析。工作台三个已确认原型仅增加 logo 返回项目管理入口；具体管理页布局待用户反馈。
- 新建示例只在本地保存文件名/大小、准备内容和阶段，不保存 PDF Blob；模型设置为模拟开关，不收集 Key。基础文件头校验不能证明 PDF 可解析；分析与跳转为固定演示，不把另一原型当作该项目的实际数据。模型配置返回保留新建输入，保存后清除搜索以显示新记录。
- 使用 domain-modeling 技能梳理 PaperDocument、文件角色与汇报 track 的边界，按仓库约定写入原架构目录，不另建 CONTEXT/ADR。Paper.documents 持有文件资源映射；页、TextBlock、SourceReference 带 documentId，Project/Paper 不再维护单 pdfAssetId。Figure/Panel 通过来源定位具体文件与页，同页码/同图号不合并身份。
- 同步创建事务覆盖全部文件、共同解析全文/图注/方法/证据、失败定位文件与页、图源连续分组、缓存及导出按文件取图、工作/旧版 Paper 保留文件映射、旧单 PDF 幂等迁移为主文档；M14/M16/M17 分别接入数据/流程/项目 UI 的实施要求。创建后继续沿原固定材料边界，不新增 PDF 替换或版本树。
- 只读代码核对显示当前 project.schema.ts 仍为单 pdfAssetId；本轮只更新设计资产，没有改运行 Schema、应用、Prompt、依赖、数据库或发布配置，也没有真实双 PDF 解析/切分/导出验收。
- Edge 1325×974 实测：搜索/无匹配、主辅文件选择与拖放、数量限制、错误后保留有效文件、未配置可保存/阻止启动、模型设置返回保留输入、刷新保留示例记录、重命名、删除取消与确认/空状态/重置、模拟分析取消无迟到完成与再次启动、工作台返回管理页均通过。截图核对列表与弹窗，无外层溢出，页面脚本错误为零。
- 静态检查通过：45 份 Markdown、176 个本地链接、代码围栏、四份有效 HTML 的脚本语法与副本一致性；四段目标模型 TypeScript 合并后 strict/noEmit 零诊断，git diff --check 通过。使用原有 Tailwind 与一次性浏览器运行时，未安装依赖、未运行应用 build/unit/PWA 或模型调用。
- 只读 fetch 无远端新增提交，保留已完成历史记录；此前远端推送目的地审批阻塞仍在，本轮不重试推送。本轮 Gate 仅设计/原型验证，真实功能留在后续另行授权阶段。

## 同阶段补充：项目阶段进度条

- 日期：2026-09-09；继续 milestone/m13，起点 eeedaf6。按用户建议将项目列表当前阶段改为图源、讲稿、幻灯片三段进度条，保留待分析、分析中、待核对等具体状态；条表示阶段位置，不表示时间完成率或导出就绪。
- 同步项目准备规格、原型索引和 HTML 副本；长时间解析迁入第一步准备状态仍为讨论建议，当前原型仍使用模拟进度弹窗，未将建议写成已确认交互。
- Edge 1325×974 验证五种阶段的当前/已推进/待开始标识、阶段筛选、可访问文本与布局通过；截图复核可读，列表及外层无溢出，页面脚本错误为零。Tailwind 编译、内联脚本语法、45 份 Markdown 的 176 个本地链接/围栏和 git diff --check 通过。
- 只读 fetch 无远端新增提交；本轮不改应用、接口、依赖或真实解析任务，不运行模型或应用测试。既有远端推送审批阻塞未解除，不重试推送。

## 同阶段补充：四步流程与论文分析查漏页

- 日期：2026-09-09；继续 milestone/m13，起点 6919c53。用户确认论文分析与图源核对拆为两个页面，整体四步；分析页展示全部页缩略图供查漏/补选，完成后必须手动点击下一步。同步产品、UI、接口、数据模型、约束、阶段工作流、执行计划及 AGENTS；不把分析完成当作切分确认。
- 新增 docs/ui/论文分析.md 与 docs/prototypes/paper-analysis.html，双文件进度与全部页网格、右侧放大、人工加入/排除/恢复自动、影响确认、局部补处理、暂停/继续/失败/限流/保存失败门槛均有演示。进入下一步前统一检查就绪，完成不自动跳页；未分析页不冒充无图。进度刷新保留滚动与焦点。
- projects.html 移除长时间分析弹窗，显式启动经一次性意图跳转到分析页，普通打开/刷新不自动续跑；支持单 PDF 与一主一补。列表变为四段阶段条，分析就绪仍在第一步，用户进入图源核对才推进第二步。三个已确认工作台只同步四步导航。
- 使用 domain-modeling 技能按现有文档目录细化页级自动判断、人工覆盖与处理版本，人工选择独立于正文范围及 PPT 取舍。新增 docs/architecture/并发与任务调度.md，分离有界本地 Worker 池与共享供应商 API 调度，明确限额、取消/迟到结果、乱序归位、内存生产约束、有限限流退避和 SDK 重试不叠乘；具体初始重试参数不是性能实测结论。
- 原型只保存示例元数据与完整模拟单元，全部缩略图/页数/模型结果均为合成；未解析真实 PDF、调用模型或实现 Worker/API 池。独立 HTML 完整跳转会停止计时，正式应用普通视图切换由项目会话保留任务；页级补选尚未传入独立 Figure 原型，不宣称完整真实链路。
- Edge 1325×974 验证：双文件 20 页/单文件 12 页、自动判断不覆盖人工补选、补选只失效对应页、已有图源排除先预览且可取消/应用、恢复自动、文件/未处理筛选与空状态、暂停后补选不复活任务、限流取消无迟到派发、失败重试、保存失败禁止下一步及恢复、刷新不续跑、完成保持分析页、按钮与导航显式进入下一步、项目记录阶段与身份交接通过；零页面脚本错误，无外层溢出。1440×974 和 1325×974 截图核对，未引入设备性能矩阵。
- 首次排除操作的自动化检查使用 uncheck，错误地期待影响确认前立即取消勾选，因此超时；改为真实点击后检查确认弹窗，再验证应用/取消，回归通过。一次文档精确替换因旧标题不匹配中止，保留已完成数据字段修改，按实际标题继续完成并通过检查。
- 静态检查：47 份 Markdown、195 个本地链接及围栏、HTML 本地跳转、五份原型脚本语法与副本一致性、四段目标模型 strict/noEmit、git diff --check 通过。只读 fetch 无远端新增提交；未改应用、Prompt、依赖、历史记录、构建或发布配置，不运行应用/模型测试；保留既有推送审批阻塞，不重试推送。

## 同阶段补充：论文分析左栏汇报要求

- 日期：2026-09-09；继续 milestone/m13，起点 8029d78。用户要求在论文分析页左栏加入汇报要求，方便修改。本轮增加材料卡片下常驻多行输入，与新建项目共用要求；独立原型单独保存，不新增领域字段。
- 输入停顿/失焦自动保存，中文输入法组合结束后提交；进度刷新不重建输入框。允许清空，失败保留草稿并提供重试；下一步和返回项目管理先保存，失败不导航。修改不重置模拟分析、不重做 PDF 或切分；后续真实用例只影响依赖偏好的生成任务与候选，保留人工稿。
- 同步论文分析/项目准备规格、接口、工作流影响表、原型索引与 HTML 副本。仅更新设计资产，未修改应用、模型、数据库、依赖或发布配置。
- Edge 1325×974 检查自动保存、刷新/清空、输入法、运行中编辑、保存失败/重试与导航门槛、项目字段共享及阶段保持通过；页面脚本错误为零，无外层溢出，截图核对左栏可读。截图只读审批首次超时，按允许范围重试一次后成功，无待解决阻塞。
- HTML 脚本语法与副本一致性、47 份 Markdown 的 195 个本地链接/围栏、git diff --check 通过；未运行应用/真实模型测试。只读 fetch 无远端新增提交，既有推送目的地审批阻塞保留，不重试推送。

## 同阶段补充：分析页确认与项目存储概览

- 日期：2026-09-09；继续 milestone/m13，起点 73025cc。用户确认分析页面不用修改，要求更新文档并在项目管理显示存储情况。
- 分析 HTML 保持原样，UI/原型索引与执行状态登记为已确认。产品与架构明确不新增文章类型确认，内部支持范围与研究设计识别保留，叙事策略作为软性写作参考，实际故事由发现、证据与汇报要求形成；没有修改 Prompt 或运行策略。
- projects.html 在列表上方增加存储概览，区分浏览器估算占用/配额与全部项目 PDF 文件信息；默认明确标注合成数据，可切换接近上限、不可用和只读浏览器估算。创建/删除更新统计，筛选不改变总量；未知值不显示零，失败或迟到结果不覆盖新状态，读取不申请持久化权限或清理内容。同步产品保存规则、UI 规格、查询接口及存储适配器边界，未增加持久化领域字段。
- Edge 1325×974 一次性浏览器验证：文件总量、筛选独立、新建/删除/取消、接近上限、不可用、合法零占用、无效配额、估算拒绝和迟到请求保护通过；无页面脚本错误和外层横向溢出，截图核对存储概览与项目列表可读。浏览器 estimate 边界使用固定替身，未将其当成真实数据库容量或磁盘性能验证。
- Tailwind 编译与内联脚本语法、原型副本正文一致性（统一换行后）、47 份 Markdown 的 196 个本地链接/围栏、HTML 本地跳转及 git diff --check 通过；git diff --exit-code -- docs/prototypes/paper-analysis.html 确认分析 HTML 未改。
- 全部原型的初次字节比较因未改分析稿副本使用 CRLF、仓库使用 LF 而失败；统一换行后正文一致，不为此改写已确认 HTML。
- 本轮仅修改文档与项目管理 HTML 设计资产；未改应用、依赖、测试、数据库、Prompt 或发布配置，未调用模型。只读 fetch 无远端新增提交；此前远端目的地的自动审批阻塞仍未解除，不重试推送。

## 同阶段补充：项目管理确认与模型配置弹窗

- 日期：2026-09-09；继续 milestone/m13，起点 7cc6ebb。用户确认项目管理页面，授权更新相关文件并制作模型配置下一页。项目准备规格、UI/原型索引和执行状态均登记确认，项目管理与四步工作台 HTML 保持不变。
- 将原公共交互 3.8 的模型表单规格完整迁入 docs/ui/模型配置.md，原节保留入口和公共状态表。延续固定候选、Key、测试、保存/清除、运行中限制及关闭返回规则；具体弹窗仍待用户反馈，未将本轮布局写成已确认。
- 新增 docs/prototypes/model-settings.html，独立载体演示全局弹窗，支持示例密钥遮蔽/显示、测试/取消、保存/失败恢复、清除和重新打开。沿 M0 记录仅展示 DeepSeek，不把未验证 GLM 加入选项；未重新核验当前供应商能力。
- 原型只使用只读示例 Key，不接收真实凭据，不发送请求。保存仅写入现有项目原型的模型演示布尔标志，不自动启动任务；现有项目页的简化模型开关保留，待本弹窗确认再统一替换，不改变新建草稿保持行为。场景选择器只用于讨论，不进入产品。
- 使用既有 Playwright/Edge 运行时做一次性验证（未新增测试框架）：1325×974 下打开、遮蔽、测试不保存、保存返回、取消草稿、认证/限流/网络失败、离线可保存、运行中禁止变更、保存失败保留旧配置、取消/关闭的迟到结果保护、单独清除通过；零模型/外部 HTTP 请求和页面脚本错误，无外层横向溢出，截图核对两栏与底部操作可读。
- Tailwind 编译、脚本语法与原型副本正文一致性、48 份 Markdown 的 205 个本地链接/围栏、HTML 本地跳转、git diff --check 通过。既有五份 HTML 无 Git 差异；仅文档与新原型，无应用/Prompt/数据库/依赖/发布改动，未调用真实模型或运行应用构建。
- 只读 fetch 无远端新增提交；既有远端目的地自动审批阻塞未解除，不重试推送。

## 同阶段补充：独立 Responses 配置页与思考强度

- 日期：2026-09-09；继续 milestone/m13，起点 1cae2ec。用户确认可填写 Base URL、模型 ID、思考强度，固定通用 Responses API，并将配置由弹窗改为独立页面；不再沿用旧固定供应商候选。新版布局待反馈。
- model-settings.html 改为独立全局页，顶部返回/保存并返回，左表单右检查结果与范围说明；API 根地址归一化并显示请求地址，默认强度不传参数的目标语义与未知档位待验证明确呈现。支持连接/能力分开检查、字段变化失效、强度不支持后显式改回默认、取消/错误恢复、只清除已保存 Key。
- projects.html 的顶栏和新建配置入口改为页面导航，移除旧模拟配置弹窗；返回恢复示例文件信息、名称和汇报要求。原型只持久化非秘密配置与 hasKey 标志，不接受真实 Key、不发送请求、不保存 PDF Blob；正式创建会话保留真实文件的要求写入 UI 规格，不能把演示元数据当作真实上传内容。
- 同步产品、UI、数据模型、接口、模型适配/旧设置迁移及执行顺序；旧运行代码仍为固定 DeepSeek Chat Completions，M0 证据不适用于 Responses。M14 增加必要接入/迁移及参数映射 Gate，M17 接入独立设置 UI；本轮未修改应用、Prompt、依赖、数据库或发布配置。
- 使用 OpenAI Docs 核对官方 reasoning 指南：effort 格式及支持档位/默认值随模型变化；架构保存来源链接，不把第三方兼容声明当作已验证能力。首次 API reference 抓取超时，改读官方 reasoning 指南成功；本地 SDK 的参数保真未实测，已列入后续 Gate。
- 既有 Playwright/Edge 一次性验证（1325×974）：无设置对话框、URL 尾斜杠与 /responses 去重、无效地址、默认/手动强度及拒绝后不降档、连接与图片能力分离、修改使结果失效、取消无迟到结果、离线/运行中限制、保存失败保留输入、保存返回/单独清除、项目管理与新建配置往返后保存示例均通过。零外部 HTTP 请求、页面错误和外层横向溢出，截图检查表单/结果/固定操作区可读。
- Tailwind 编译、两份脚本与副本正文一致性、48 份 Markdown 的 208 个本地链接/围栏、HTML 本地跳转、旧候选/弹窗规则残留搜索及 git diff --check 通过。保留四步 HTML，不运行应用/真实模型/供应商或性能测试；只读 fetch 无远端新增提交，既有推送目的地审批阻塞保留，不重试推送。

## 同阶段补充：独立模型配置页确认

- 日期：2026-09-09；继续 milestone/m13，起点 59c1cee。用户确认设置页面，要求更新相关文档。
- 模型配置规格、UI/原型索引、公共入口和执行状态登记当前独立配置页已确认，涵盖 Responses API、Base URL、模型 ID、思考强度及连接/能力检查、保存/返回与恢复交互；历史讨论记录保留，不改写先前尚未确认的事实。
- 本轮仅修改文档，全部 HTML 原型和应用/Prompt/依赖/测试保持不变；模型接入仍待后续阶段实施，不把页面确认当作真实 Responses 能力验收。
- 静态检查通过：48 份 Markdown、208 个本地链接、代码围栏与 git diff --check；原型目录 HTML 无 Git 差异。没有运行浏览器、应用或真实模型测试，复用前一轮原型验证记录。
- 只读 fetch 无远端新增提交；既有远端目的地自动审批阻塞仍未解除，不重试推送。

## 同阶段补充：用例审视与独立添加 Figure

- 日期：2026-09-09；继续 milestone/m13，起点 d830e69。依据用户确认的讨论同步文档与原型，未开展 M14 应用实现。
- 图源页增加独立“添加 Figure”，选原页并框选后创建整图；更多菜单添加跨页图块归入当前 Figure。“整图边界”只调整已有图块，共用八手柄、裁切预览和撤销；允许无 Panel 整图确认。模型补充未执行的状态明确展示，新增素材仍是合成示例。
- 数据契约增加 FigureRegion，区分逻辑整图、单页图块与 Panel；同步来源、引用展开、旧单图迁移、人工补整图及成功处理但漏定位的恢复入口。新增查询明确返回整图集合或指定单页来源，不默认取跨页图的第一页。
- 讲稿增加稳定 SpeechParagraph 与可分配 SpeechSegment，分页保留段落及章节，备注从唯一正文派生。页面更新建议和合法未分配内容只提醒，非法引用/重复分配/备注错误及溢出仍阻断导出。slides.html 的跨章合页和待安排讲述分配移除隐式章节迁移。
- 分析完成单元与 inputKey、成果同事务保存，刷新后用户继续缺失/过期单元；整阶段 checkpoint 仍独立，计划/Deck 不扩展为半稿保存。同步模块职责、接口、恢复规则、UI 和 M14–M17 必要交付范围，未另行调整阶段顺序。
- 既有 Playwright/Edge 一次性验证（1325×974）：新增/取消/调框、键盘微调、Esc 取消拖动、撤销/重做、刷新保留、跨页共用 Figure 身份、追加 Panel、无 Panel 确认、侧栏调宽后严格裁切、存储不可用提示、标签转义、跨章合页保留讲述归属通过；无页面错误、外部 HTTP 请求或外层横向溢出。首轮发现目录平滑滚动未结束时可能选中上一图，改为点击立即定位并选中后同一主链复查通过。截图检查新增对话框与工作台布局可读。
- 静态检查：Tailwind 编译、两份原型脚本语法、数据模型四段 TypeScript 合并后 strict/noEmit 检查通过；48 份 docs Markdown 加 AGENTS.md 共 49 份，214 个本地链接、UTF-8、围栏与 HTML 相对导航通过；原型副本一致性和 git diff --check 通过。
- 原型使用独立演示状态，存储失败仅保留本次页面并明确提示；不代表正式应用事务已实现。没有改动应用、Prompt、依赖或测试，未调用模型、解析真实 PDF、验证真实续跑/切分准确率或运行性能基准；后续阶段仍须按 Gate 验收。
- 只读 fetch 无远端新增提交；既有远端目的地自动审批阻塞未解除，不重试推送。

## Gate、限制与下一步

M13 文档 Gate 通过，包含目录重构、接口/数据约束、图源原型与本次已确认的分步流程及已确认的大纲/幻灯片 HTML 交互稿，检查导出已按用户确认并入幻灯片；最新四步流程、分析页查漏与手动下一步、计算池/API 调度设计已同步，分析 HTML 已获确认，项目管理及存储概览已获确认，Responses 独立配置页及思考强度、连接/能力检查的布局与交互已获确认。现行权威定义已更新，历史验收的适用版本保持清楚；新目标没有标为已实现。

没有修改 src、prompts、tests、package.json、lockfile、构建或 Pages 工作流，没有安装依赖、调用真实模型、重新测试参考项目或运行应用 build/browser/PWA。文档类型检查不代表运行 Schema、切分准确性、工作台 UI 或一键生成已验证。

本次起始工作区干净，未读取或吸收额外任务书。旧里程碑记录保持原样，不预建 M14–M18 工作记录。按现有 Git 流程提交、合并并仅推送 main 后停止；下一步仅在另行授权时实施 M14。
