# M8 工作记录（进行中）

- 里程碑：M8 行为保持的模块化与可读性重构
- 日期：2026-09-06
- 工作分支：milestone/m8
- 目标及完成状态：整体进行中，未达 M8 Gate，不合并 `main`。已完成子步骤：M8.1「拆分领域 Schema 和 Type」、M8.2「拆分 storage.ts 与提取 repository」、M8.3「拆分 ai.ts 与建立 Assistant 模块边界」、M8.4「拆分 generation.ts 与建立 Generation Pipeline 边界」、M8.5a「Deck Domain 与最小 Source 前置收口」、M8.5b「Supporting Modules 行为保持迁移」、M8.5c「Application / Controller Boundary」；未进入 M8.5d。

## M8.1：拆分领域 Schema 和 Type（已完成）

将 `src/types.ts` 按领域拆分为六个文件，全部定义逐字搬迁，未修改任何字段语义、默认值、枚举、序列化格式或校验规则：

- `src/shared/schema.ts`：`BBoxSchema`/`BBox`，作为 paper 来源、deck 裁图覆盖与 PDF/布局/UI 换算共用的几何契约（唯一进入 shared 的类型，满足“多领域共用且不属单一领域”）。
- `src/modules/project/project.schema.ts`：`Checkpoints`、`ProjectSchema`/`Project`、`PdfAsset`。
- `src/modules/paper/paper.schema.ts`：`SourceReference`、`FigurePanel`、`FigureRef`、`Claim`、`Evidence`、`Story*`、`StudyProfile`、`Metadata`、`Paper`。
- `src/modules/deck/deck.schema.ts`：`LayoutIds`/`SlideKinds`、四类元素 Schema、`Slide`、`Deck`、`RevisionScope`、`DeckMutation`、`ApplyRevisionArgs`、`RevisionRequest`、`RevisionRecord`。
- `src/modules/outline/outline.schema.ts`：`DeckPlanSchema`/`DeckPlan`。
- `src/modules/assistant/assistant.schema.ts`：`ChatMessage`。

其他调整：

- 删除 `src/types.ts`；更新 16 个 `src/` 文件与 3 个测试文件的 import；`src/deck.ts` 的类型再导出改指向 `modules/deck/deck.schema`。
- 模块依赖方向：`paper`/`deck` → `shared`，`outline` → `deck`，`project`/`assistant` 独立；无循环依赖、无 paper→deck 反向依赖。未创建 barrel `index.ts`、基类或新抽象。
- 唯一非搬迁内容：`BBoxSchema` 上新增一行 JSDoc，说明整页坐标系不变量（纯注释，无运行时影响）。

## M8.2：拆分 storage.ts 与提取 repository（已完成）

将 `src/storage.ts`（326 行）拆为 IndexedDB 基础设施与领域 repository，函数体逐字搬迁（逐行对比确认仅增加跨模块必需的 `export` 关键字），随后删除 `src/storage.ts`，未保留兼容层：

- `src/shared/persistence/indexedDb.ts`：`DATABASE`（`smartjc`）、store 清单、`request`/`open`/`transaction`/`get`/`stored`。数据库版本仍为 1，upgrade 回调与七个 store 原样；不 import 任何领域模块。
- `src/modules/project/projectRepository.ts`：`projectIn`、`createProject`、`listProjects`、`loadProject`、`updateProject`、`deleteProject`、`saveStage`（checkpoint 推进属 Project 生命周期）及 `ProjectData`/`StageCapture` 类型。
- `src/modules/paper/paperRepository.ts`：`readProjectScoped` 与 `getPaper`/`getPaperPage`/`getPaperFigure`/`getPaperClaim`（AI 只读工具的项目内论文读取）。
- `src/modules/deck/deckRepository.ts`：`captureVersion`、`commitRegeneration`、`restorePrevious`、`saveRevision`、`captureRevision`、`getDeck` 及 `VersionCapture`/`RevisionReadContext` 类型。
- `src/modules/assistant/conversationRepository.ts`：`loadHistory`、`saveConversation` 与 history store 助手 `assertMessage`/`trimHistory`。
- `src/shared/llm/settingsRepository.ts`：`loadSettings`/`saveSettings`（模型设置属 LLM 基础设施，非 assistant 领域）。

原子事务处理：`createProject`、`loadProject`、`updateProject`、`deleteProject`、`saveStage`、`saveRevision`、`commitRegeneration`、`restorePrevious`、`saveConversation` 等跨 store 用例均保留原有单次 `transaction([...stores], mode, ...)` 边界，未拆分为多个事务；跨领域读取/裁剪通过在同一事务内调用共享助手（`projectIn`、`readProjectScoped`、`assertMessage`、`trimHistory`）完成。plans store 无独立生命周期函数（仅随 loadProject/saveStage/deleteProject/commitRegeneration 事务读写），按真实归属不创建 outlineRepository。

调用方迁移：`src/ai.ts`、`src/generation.ts`、`src/ui/App.tsx`、`src/ui/HomePage.tsx`、`src/ui/ProjectPage.tsx`、`src/ui/SettingsDialog.tsx`、`src/ui/editor/AiPanel.tsx` 与 4 个契约测试改从对应 repository 导入；`tests/browser.mjs` 中 19 处页面内动态导入同步改为新路径（测试脚手架引用，非应用代码）。

## M8.3：拆分 ai.ts 与建立 Assistant 模块边界（已完成）

将 `src/ai.ts`（266 行）按职责拆入 assistant 模块，函数体逐字搬迁（与旧文件逐行对比，差异仅为跨文件必需的 `export`、读写工具按领域拆成两份、runtime 内合并 schema 与双分支分发），随后删除 `src/ai.ts`，未保留兼容层：

- `modules/assistant/target/resolveTarget.ts`：`AiTarget`/`AiRecentMessage`、页码与章节解析、`resolveSlideTarget`/`resolveAiTarget`、`modificationRequest`（自然语言目标解析，纯逻辑）。
- `modules/assistant/tools/paperReadTools.ts`：paper.* 四个读工具的 schema、描述与执行。
- `modules/assistant/tools/deckReadTools.ts`：`deck.get` 读工具。
- `modules/assistant/tools/revisionTool.ts`：`revisionToolSchema`（`deck.apply_revision` 工具参数 schema）。
- `modules/assistant/revision/validateRevisionProposal.ts`：`validateAiCandidate`（参数/scope/Figure 校验 + DeckSession 内存模拟，不产生存储写入）。
- `modules/assistant/runtime/runAssistant.ts`：手写 10-turn LLM/tool 循环 `runAiRevision`、意图分类 schema、上下文组装与工具分发，全部保持原语义，为 M10 替换 pi-agent-core 隔离。

工具契约对比确认：五个读工具与 `deck.apply_revision` 的名称、参数 schema、描述、注册顺序、10 轮上限、stop 条件、toolResult 消息格式、错误消息全部不变。

配套调整（本轮任务书授权）：

- M8.2 遗留的 `assertMessage`/`trimHistory`（含 `HistoryEntry`/`isMessage`）从 conversationRepository 迁入 `src/shared/persistence/historyStore.ts`（对模块仅 type-only 引用）；deckRepository 与 conversationRepository 共同依赖该中性 helper，deck→assistant 的运行时依赖消除（deckRepository 仅余 `import type { ChatMessage }` 编译期签名引用）。
- `src/model.ts`（84 行纯 LLM 基础设施）迁入 `src/shared/llm/model.ts`，与 settingsRepository 同处；更新全部 import（含 Editor.tsx 的内联类型导入、browser.mjs 动态导入、pwa.mjs 文本读取路径），provider 行为不变。

调用方迁移：`src/ui/editor/AiPanel.tsx`（runAiRevision）、`tests/ai-contracts.ts`（按 target/revision/runtime 拆分导入）；model 路径更新涉及 analysis/generation/App/SettingsDialog/ProjectPage/Editor/AiPanel、settingsRepository 与三个测试文件。未创建 barrel、工具框架或 Agent 抽象层。

## M8.4：拆分 generation.ts 与建立 Generation Pipeline 边界（已完成）

将 `src/generation.ts`（134 行）按 Planning / Building / Orchestration / Regeneration 拆入 `src/modules/generation/`，函数体逐字搬迁（与旧文件逐行对比，差异仅为 `paperContext`/`assignPlanIds` 的跨文件 `export`），随后删除 `src/generation.ts`，未保留兼容层：

- `modules/generation/layoutRules.ts`：`layoutRules` 布局约束常量（assistant runtime 改从此处导入，不再依赖生成编排）。
- `modules/generation/planDeck.ts`：Planning——`paperContext`（模型上下文不携带全文 pages）、`assignPlanIds`（计划 ID 分配与校验）、`planDeck`（Paper + 策略 + plan 提示词 → DeckPlan）。
- `modules/generation/buildDeck.ts`：Building——`GenerationOutputSchema`、`assembleDeck`（确定性组装与图源一致性校验）、`generateDeck`（计划 + generate 提示词 → Deck）。
- `modules/generation/runGeneration.ts`：Orchestration——`GENERATION_STEPS` 与 `generateProject` 的 checkpoint while 循环（project-created → pdf-parsed → figures-ready → paper-ready → deck-plan-ready → deck-ready，逐字保留）。
- `modules/generation/regenerateDeck.ts`：Regeneration——`RegenerationPlanSchema` 与 `regenerateProject`（内存重新规划制作，成功后一次 `commitRegeneration` 切换版本）。

配套：`src/analysis.ts`（60 行，Figure 分析与论文理解，架构目标树明确属 paper 模块）以纯移动方式迁至 `src/modules/paper/analysis.ts`，仅调整 import 路径。调用方迁移：`ProjectPage.tsx`（runGeneration + regenerateDeck）、`runAssistant.ts`（layoutRules）、`tests/generation-contracts.ts`、`tests/analysis-contracts.ts`。

Checkpoint 名称、顺序、saveStage 调用时机（每阶段一次、共五处）、resume 起点、取消路径、3 处 requestJson 模型调用、prompt 拼接与错误消息全部不变；`indexedDB.open` 仍仅在 shared/persistence。

## M8.5a：Deck Domain 与最小 Source 前置收口（已完成）

将 `src/deck.ts`（105 行）拆入 `src/modules/deck/` 并按架构 Review 收口跨领域边界，随后删除 `src/deck.ts`，未保留兼容层：

- `modules/deck/DeckSession.ts`：DeckSession 会话（提交、版本、Undo/Redo、requestId 去重、stale 防护）。`RevisionOptions` 移除 `messages?: ChatMessage[]`，DeckSession 不再感知 Assistant 消息；新增 deck 领域自有的可选单次提交 `persist` 覆盖（`RevisionCommitOptions`），供应用层组合提交使用。
- `modules/deck/mutations.ts`：`applyMutation`、`findSlide`、`ensureScope`、`createSlide` 等修改原语（逐字搬迁）。
- `modules/deck/validateDeck.ts`：`validateDeck` 自 `src/layout.ts` 迁入（逐字搬迁，figure 校验改用 locator）。
- `modules/deck/layoutRules.ts`：`layoutRules` 常量（自 generation 迁入）与 `layoutCapacity`（自 layout.ts 迁入）；Generation 与 Assistant 均改为依赖 Deck layout contract。

Assistant 修订的原子提交保持：新增 `modules/assistant/revision/applyRevision.ts` 的 `applyAssistantRevision` 薄命令，通过 persist 覆盖把可见对话与 Deck 修订组合到同一次 `deckRepository.saveRevision` 事务；ProjectPage 仍是信号组合（`AbortSignal.any`）与 projectId 的唯一所有者，经 Editor/AiPanel 以 `persistRevision` prop 传入 `runAiRevision`。stale/重复/取消/失败回滚语义逐字不变。

Source 收口：`sources.ts` 的 `figureSource` 改收 `FigureSourceLocator { figureId; panelId? }`，Paper 来源解析不再接受 Deck 元素；`validateDeck`、`export.ts`、`pdf.ts` 在调用侧做最小适配。`paperRepository.readProjectScoped` 私有化，`deckRepository.getDeck` 自行声明只读事务并复用 `projectIn`，deck→paperRepository 依赖消除。`src/layout.ts` 保留 computeLayout/validatePlan 等（M8.5b 处理）。

与旧 `deck.ts` 逐行对比：差异仅为 `messages` 移除、`persist` 覆盖选项、跨文件 `export` 关键字与 `clone` 随文件各存一份；校验、scope、undo/redo 逻辑逐字一致。Deck 领域对 Assistant 仅剩 `deckRepository` 的 `import type { ChatMessage }`（history store 持久化形状，编译期擦除）。

## M8.5b：Supporting Modules 行为保持迁移（已完成）

将六个 `src/` 根目录支撑模块按 ownership 迁移（git 均识别为 rename，函数体逐字搬迁，仅调整 import 路径与下列说明的最小适配），旧文件全部删除、无兼容层：

- `src/layout.ts` → 拆分：`computeLayout`/文字测量/几何类型 → `modules/deck/layout/computeLayout.ts`（预览与导出继续共用同一几何实现）；`validatePlan` → `modules/outline/validatePlan.ts`（v1 校验逐字保留，M9 才升级）；`validateBBox` → `shared/schema.ts`（与 BBoxSchema 同处的纯几何约束）；`layoutIds` 随 computeLayout 迁移（当前无调用方，保留待后续清理）。
- `src/export.ts` → `modules/deck/export.ts`（exportDeck/downloadDeck 逐字；PPTX 行为、几何换算、来源收集不变）。
- `src/sources.ts` → `modules/paper/sources.ts`（figureSource/validatePaper/sourceText 逐字），并新增 `figureImage(resource, paper, locator, cropOverride?, edge?)` 薄适配：定位/覆盖框解析在 Paper 侧，渲染裁图交给 shared/pdf。
- `src/pdf.ts` → 拆分：`shared/pdf/pdfResource.ts` 承担 PDF.js 生命周期、页面渲染、图片区域检测、裁图缓存（`image` 改收 `{ id, pageNumber } + BBox`，缓存键与默认边长不变；不再 import Paper/Deck）；Paper 解析组装（页文本循环、<100 字符检查、元数据标题、文字来源生成）提取为 `modules/paper/parsePaper.ts`，`runGeneration` 改调 `parsePaper(resource, paper, signal)`。
- `src/prompts.ts`/`src/prompt-config.ts` → `shared/llm/`（glob 相对路径随位置改为 `../../../prompts/**`；解析正则基于路径后缀，加载内容与校验行为不变）；`scripts/check-prompts.ts` 同步。
- `src/fixtures.ts` → `tests/fixtures.ts`（测试与开发演示数据；`src/ui/FixturePage.tsx` 为 DEV-only 入口，其引用经 `import.meta.env.DEV` 门控，不进入生产构建）。

调用方迁移：`SlidePreview`/`Editor`（deck/layout）、`PdfPageView`/`SourceDialog`/`HomePage`（shared/pdf）、`ProjectPage`（figureImage 适配 + deck/export 动态导入）、generation/paper/deck/project/assistant 模块与四个契约测试、`browser.mjs`、`pwa.mjs`。

## M8.5c：Application / Controller Boundary（已完成）

将大型 React 组件中的异步业务流程提取为 controller hook（逻辑逐字迁移，JSX、样式、交互与文案不变），页面组件回到"取状态 → 渲染 → 传 action"：

- `src/ui/project/useProjectWorkspace.ts`：项目加载、PdfResource 打开与切换/卸载释放、旧任务中止（原 ProjectPage 外层 effect 逐字）。
- `src/ui/project/useProjectController.ts`：DeckSession 构造与修订持久化组合（persistRevision）、偏好保存队列（savePreferences 链）、生成/重生成/恢复任务（AbortController、阶段/警告/错误状态）、导出命令（exportPresentation）、leave guard、来源与重生成对话框状态。
- `src/ui/project/ProjectPage.tsx`：渲染组件（自 src/ui/ProjectPage.tsx 迁入）；`useEditorController.ts`：Deck 会话命令（commit/select/move/history/addSlide/addElement/source 裁图）、草稿保存队列（draft/pending/flush 递归、beforeunload、状态机）、AI 接线（manualEdit/cancelAi/registerAiCancel）与导出任务；`Editor.tsx` 保留渲染、ResponsivePanel 与面板 UI 状态。
- `src/ui/editor/useAssistantController.ts`：输入草稿、历史加载、AI 请求任务与取消、摘要撤销（undoRevision）；`AiPanel.tsx` 回到纯渲染。M10 替换 runtime 时 AiPanel 只依赖 controller 的 `send/cancel`。
- HomePage（70 行，list/upload/rename/delete 线性流程）按真实职责保留现状；未创建转发型 wrapper。

app 层归位：`src/activity.ts`、`src/pwa.ts`、`src/ui/App.tsx` 迁至 `src/app/`（activity/pwa 为 git rename；App 仅调整相对导入与 ProjectPage 懒加载路径）；`src/main.tsx` 保留为 6 行薄 bootstrap（引用 `./app/App`），`index.html` 入口不变。

异步生命周期逐字保持：项目切换 dispose、StrictMode 下 effect 幂等、stale 回调经 aborted/任务标识双检、偏好保存队列串行、flush 递归与 IME composing 拦截、AI 手工草稿失效、重生成取消不写入。组件中仅剩 controller hook 与局部 UI 状态；FixturePage（DEV-only）的固定 DeckSession 与 SettingsDialog/PdfPageView 的局部 AbortController 为原有合理职责。

## 检查（2026-09-06，Windows，仓库既有脚本）

M8.1–M8.5b 后各运行一轮，M8.5c 后结果：

- `npm run build`：通过（prompts 检查 + `tsc --noEmit` + `vite build`）。
- `npm run check:browser`（vite dev 5174 + headless Edge）：10 组全部通过，含原子创建/阶段/修订、证据映射、生成与重生成原子性（含取消写入、计划清理、版本交换）、AI 目标与提案、固定 SSE、React 草稿/输入法/撤销、上传主链、AI 面板、重生成/恢复、PDF/裁图/导出。
- `npm run check:pwa`（`dist` 构建 + 本地固定服务）：4 组全部通过。
- 旧 `storage`/`ai` 引用搜索（`rg`，含动态导入与内联 `import()` 类型引用）：0 残留。
- 新旧函数逐行对比（M8.2 对 storage、M8.3 对 ai）：非 import 行逐字存在，差异仅为拆分必需的 `export` 关键字、读写工具按领域拆分及 runtime 内合并分发；工具名称/schema/描述/循环与错误消息全部一致。
- `indexedDB.open` 搜索：应用代码仅在 `shared/persistence/indexedDb.ts` 打开（名称/版本/upgrade 不变）；测试文件的直连打开为原有断言脚手架。
- `git diff --check`：通过。
- 环境说明：仓库未安装 playwright，通过脚本支持的 `SMARTJC_PLAYWRIGHT_MODULE` 指向本机另一项目的 playwright 安装运行，未给仓库新增依赖。

## Gate 结论

M8 整体未完成：格式/lint 工具与 `format`/`lint`/`test`/`check` 脚本、必要全仓格式化与 M8 Gate 复查（含模块依赖方向终检与高密度单行改善复核）尚未交付。本轮通过的是 M8.1–M8.5c 的行为保持验证，不作为 M8 Gate 通过；分支保留在 `milestone/m8`，不合并、不推送。

## 未解决问题与下一步

- 视觉组件重组（步骤导航、章节导航、Inspector、CommandBar、proposal diff、工作区布局）明确推迟到 M11。
- `layoutIds` 当前无调用方；`deckRepository` 的 `import type { ChatMessage }` 纯类型引用；`saveStage` 归属与 `ChatMessage` 手写 type 均维持现状待后续复评。
- M9 落点：Plan 修改在 `modules/generation/planDeck.ts` + `modules/outline/outline.schema.ts`，Plan 校验在 `modules/outline/validatePlan.ts`；deck-plan-ready 暂停与 prepare/build 拆分在 `modules/generation/runGeneration.ts`；Build 在 `modules/generation/buildDeck.ts`；checkpoint 保存由 `projectRepository.saveStage` 承担；PDF 基础设施在 `shared/pdf/pdfResource.ts`，Paper 解析组装在 `modules/paper/parsePaper.ts`。M10 替换 runtime：AiPanel → useAssistantController → runAssistant，提交链为 `applyAssistantRevision → DeckSession.applyRevision(persist) → saveRevision`。
- 下一步为 M8.5d（Tooling & M8 Gate：格式/lint 工具、Vitest、`format`/`lint`/`test`/`check` 脚本与 Gate 复查），待下一轮授权后启动。
