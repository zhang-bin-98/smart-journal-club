# M4 完整生成与阶段续跑

- 日期：2026-09-06
- 工作分支：`milestone/m4`
- 状态：已完成；Gate 通过
- 目标：把上传、论文解析、图源分析、论文理解、汇报规划和完整 Deck 生成接成一条可取消、可续跑的主链。

## 完成内容

- 在 `src/types.ts` 增加实际 `DeckPlanSchema`；`src/layout.ts` 增加计划与跨 Paper 引用校验。计划只保存页目的、布局、Claim/Source 和 Figure/Panel 引用，完整 Deck 生成后从 `plans` 删除。
- 新增 `src/generation.ts`，统一串接五个阶段：解析论文、分析 Figure/Panel、理解研究内容、规划汇报结构、制作幻灯片。提示词新增 `prompts/stages/plan.md` 和 `prompts/stages/generate.md`，仍复用公共规则和单一研究策略。
- `src/storage.ts` 扩展计划及完整 Deck 的原子阶段提交，事务内重新核对项目、Paper、PDF、前置 checkpoint 和有效计划；取消、旧标签页、删除项目或写入失败不会推进阶段或删除最近完整计划。
- 项目准备页收敛为“生成 PPT / 继续生成”，显示高层阶段；完成后直接进入编辑器。编辑器保留图源和裁图入口，并显示生成后的图源、文字与裁图核对提示。
- 预览和 PPTX 导出共用动态文字测量结果，长标题会缩小到最低字号并在仍可能溢出时提示；修正图文布局辅助文字高度，避免多段文字重叠。
- `PdfResource.imageRegions` 从 PDF.js 页面操作矩阵提取实际栅格图区域，并作为 Figure 分析的坐标参考。此前重复应用源图片像素尺寸造成的坐标放大已修正。

## 实际检查

- `npm run build`：通过；提示词校验、TypeScript 和 Vite 构建通过。保留依赖已有的 Zod Rollup 注释 warning。
- `npm run check:browser`：通过。新增固定响应覆盖 DeckPlan 校验、图源引用、完整 Deck 组装、计划清理、阶段写入失败、写入期间取消、旧阶段和删除项目；浏览器主链覆盖上传、五阶段、取消、刷新后仅继续未完成阶段、编辑器进入、裁图隔离、Undo/Redo、导出与重开。使用 Edge，Chrome 未安装。
- 真实模型主链：使用已保存的组学 Paper 复用分析结果，先完成一次 17 页中文 Deck 的规划、生成、PPTX 导出和刷新重开；由于发现真实截图中的部分 Panel 坐标偏移，修正 PDF.js 图片区域参考后重新从上传开始生成，得到 16 页 Deck。重新生成的 Figure 3 Panel D 截图包含柱状图、坐标轴和图例，标题与副标题不重叠；模型请求均返回 HTTP 200。
- 真实输出继续保留用户可核对的来源页码和当前元素裁图；自动定位不能替代科学内容复查，生成完成后的页面仍提示核对主要结论、图例和裁图边缘。

## 未解决问题与范围

- 组学样例的最终生成内容已检查代表性结果页，但机制和临床样例只复用 M3 的 Paper 结果，完整 Deck 的人工内容复查留到 M6 统一完成；不把分析结果等同于三篇最终 Deck 验收。
- 依赖审计中 PptxGenJS 传递的 `image-size` advisory 沿用 M2/M3 记录；本阶段未升级依赖。
- 本阶段未实现 AI 问答/受控修改、整套重生成/上一版恢复、PWA 离线与 Pages 最终发布检查，这些属于 M5/M6。

## Gate

通过。代表论文已从上传一路生成完整 Deck，阶段产物可保存，取消和刷新后从最近完整阶段继续，已完成阶段不会重跑；来源查看、当前元素裁图、手工编辑、导出和重开沿同一主链可用。
