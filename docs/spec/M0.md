# M0 技术路线验证

- 日期：2026-09-05
- 状态：部分完成，模型/PDF/本地导出检查通过；Pages Gate 未通过
- 工作分支：`milestone/m0`
- 目标：建立单一 Vite 浏览器验证入口，验证 PDF、共享布局、PPTX、IndexedDB 和模型/Pages 路线。

## 已完成

- 创建 TypeScript + Vite 单页入口，固定 3 页 Deck fixture 可切换预览。
- 建立 `BBox`、六类布局、Figure 引用和基础 Deck 校验；Web 预览与 PptxGenJS 导出共用 `computeLayout` 的归一化框。
- 接入 PDF.js 6.3.289 的页文本、首页渲染和随构建分发的 worker；按同一 Source bbox 生成裁图。
- 接入 IndexedDB `assets` store，保存和重读原 PDF Blob。
- 接入 PptxGenJS 4.0.1 导出；文字为文本对象，Figure 为等比图片。
- 增加 `.env` 忽略规则。已有本地 Key 未读取、未写入仓库或构建产物。

## 验证

环境：Windows / PowerShell，仓库根目录。

- `npm install`：通过；安装 Vite 7.3.6、TypeScript 5.9.2、PDF.js 6.3.289、PptxGenJS 4.0.1、Zod 4.5.4。
- `npm run build`：通过；`tsc --noEmit` 和 `vite build` 均通过，PDF.js worker 进入 `dist`。
- `git diff --check`：通过。
- 本地 dev server：`http://127.0.0.1:5173/` 已启动，Hash 入口使用相对资源。
- 真实 Edge 浏览器加载 `http://127.0.0.1:5173/#/m0`：固定 Deck、三页切换和 Hash 刷新入口正常。
- 公开 PLOS Biology 原创研究 PDF（临时目录，3,257 KB）：PDF.js 解析 33 页、首页文本 4,617 字符并完成渲染；同一 Source bbox 裁图成功；IndexedDB Blob 写入并可重读。
- PPTX 下载得到 `smartJC-fixture.pptx`；解包检查包含 3 个 slide XML，文字对象数为 3/5/2，图片对象数为 0/1/2；PowerPoint 以“受保护的视图”打开，状态栏确认“幻灯片第 1 张，共 3 张”，文档文本可见；未启用编辑，因此文字可编辑性仍未完全确认。
- DeepSeek `deepseek-v4-flash-vision-exp` 真实浏览器请求：Vision + JSON HTTP 200/CORS `cors`；Streaming 首块可读；Abort 返回 `AbortError`；显式关闭 thinking 后 Function Calling HTTP 200 且收到 tool call。Key 只从本地 `.env` 读取，未打印或写入仓库。

## Gate 与未验证项

Gate 未通过。DeepSeek 浏览器路线已通过上述能力检查；GLM 未配置 Key，未验证。代表论文样例已用公开 PLOS Biology PDF 完成 PDF.js/裁图/Blob 检查；PowerPoint 已打开导出文件，但处于受保护视图，未完成文字编辑确认。远程已配置为 `zhang-bin-98/smart-journal-club`，目前远程没有分支或 Pages 发布产物，子路径、刷新和 worker 尚未验证。模型官方限制记录在执行计划与架构文档中。

## 下一步

下一步是将当前分支按用户授权推送到 `origin` 并配置 Pages，随后在实际子路径检查 Hash 刷新、静态资源和 PDF.js worker；PowerPoint 人工打开确认完成后再决定 M0 是否通过。GLM 只有在提供对应 Key 时再测，不引入代理。
