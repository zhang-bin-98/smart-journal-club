# M10 工作记录

- 日期：2026-09-07
- 工作分支：`milestone/m10-work`
- 状态：进行中，尚未通过 Gate。

## 已完成

- 接入 `@earendil-works/pi-agent-core` 0.85.1，使用 `Agent`、顺序工具执行、事件订阅、取消和轮次终止 hooks。
- 提问模式仅注册论文/大纲/Deck 读取工具；修改模式额外注册唯一 `deck_propose_revision`。
- 修改工具只在内存工作副本生成 `PendingRevision`；新增应用命令在应用前重新核对项目、Deck、revision、scope、引用和布局，成功后复用 `DeckSession` 的原子提交与一次 Undo。
- 编辑器新增提问/修改模式、作用范围、实际范围、提案差异、应用/放弃控件和运行状态。
- 更新研究助手提示词及 AI 工具参数，删除旧的独立意图分类和手写 Agent turn loop。

## 检查

- `npm run typecheck`：通过。
- `npm run check`：通过，14 个文件、160 项单元测试、生产构建。
- `git diff --check`：待收尾时执行。
- `npm run check:browser`：失败。执行 AI 合同时页面上下文被销毁，尚未定位 Agent 流适配的运行时问题，因此不能宣称浏览器 Gate 通过。

## 未完成与下一步

- 修复浏览器 AI 合同中的上下文销毁，补齐真实 UI 的提案等待应用、取消、手工草稿失效、过期版本和应用后 Undo 验收。
- Gate 通过后再推进 M11 编辑工作区与检查导出页。
