# M7 v0.4 设计契约统一

- 日期：2026-09-06
- 工作分支：milestone/m7
- 目标：统一产品、架构、实施顺序与工作约定，只交付文档。
- 状态：文档修订完成，静态 Gate 通过；v0.4 应用功能尚未实施。
- 基线：从与 origin/main 一致的 main（02817f2）创建分支；M0–M6 完成历史和原有未跟踪任务书保留。

## 完成内容

- [产品文档](../产品文档.md) 升至 MVP v0.4，统一论文理解、可编辑大纲、确认后制作 Deck、检查/导出的用户闭环；补齐步骤开放、返回、草稿/确认/失败状态及线框，改为章节导航、Canvas、Inspector 和可收起 AI。保留来源/裁图、设置、输入草稿、零页、撤销、版本及离线交互。
- [架构文档](../架构文档.md) 升至 MVP v0.4，明确 Section、DeckPlan/Deck v2、PlanMutation/OutlineSession、计划持久化及确认版本、prepare/build 与候选重生成、迁移及叙事校验的唯一契约；保留证据链、共享几何、原子提交和 Current/Previous。
- 架构同时明确 app/modules/shared 的目标职责与依赖方向，固定 workflow 与交互式 Agent Core 分工，以及只读提问、唯一 proposal 写意图工具、用户应用、事件、取消和版本保护。
- [执行计划](../执行计划.md) 保留 M0–M6 的历史交付及记录，加入 M7–M12 的目标、禁止项与 Gate；更新同一条 v0.4 主链和原三篇论文的人工叙事检查。M8 明确为行为保持，M9 才改 Schema/流程，M10 才接 Agent Core，M11 改编辑工作区，M12 做最终发布验收。
- [AGENTS.md](../../AGENTS.md) 保留沟通、Git、自动提交/合并及只推 main 的授权，增加真实职责模块化、依赖边界、格式与可读性、固定响应及不过度设计约束。

## 冲突消除与必要决定

- 修订自动贯通到编辑器、大纲不可编辑/无需确认、固定常驻聊天栏、AI 修改直接应用、业务模块平铺、策略无基础顺序约束，以及 M6 后没有明确下一阶段的旧约定。历史版本的实现描述仍只在历史区和原工作记录保留，不是 v0.4 生效规则。
- 按任务书目标选择一份待消费计划：候选计划可恢复，生成成功才消费并交换版本；不维护 Current/Previous 的计划版本树。字段细节只在架构第 2、4、5 节定义。
- 为落实已有 Deck 时重新分析不能破坏证据链，采用同一 PDF 的独立新项目入口；尚无 Deck 的重新分析原子替换分析和计划。此为本轮明确的最小隔离设计，未实现，产品第 3.4 节与架构第 4 节一致。
- 为保证删除临时计划后仍可查看页目的，Slide 保留可选 purpose；旧稿不猜测该字段。计划增删章节、草稿保存、确认失效、零页/旧稿修复与叙事门槛在原契约段落补齐，没有新增计划历史树或科研评分平台。

## 实际检查

环境：Windows / PowerShell，仓库已有 Node.js 与 TypeScript；命令在仓库根目录执行。默认执行环境曾在初始化时失败，受审核的执行方式恢复后完成以下检查，无遗留环境阻塞。

- git status --short、git branch --list、git remote -v、git log -3 --oneline：核对 main、既有里程碑分支与用户未跟踪任务书；没有其他初始未提交改动。
- git fetch origin main、git rev-list --left-right --count main...origin/main：开始与收尾前均为 0/0；从该 main 创建 milestone/m7。
- rg --files 与分段完整阅读：任务书、AGENTS.md、三份权威文档、设计基线和 M0–M6 工作记录已读；实际 src 文件结构、package.json 与类型定义仅用于只读核对。
- rg -n：逐项搜索任务书要求的八类旧措辞及上下文；四份现行文档无指定旧冲突，历史阶段行为与 v0.4 明确分开。
- node --input-type=module（一次性脚本经标准输入运行，不写入仓库）：四份文档的 UTF-8、代码块、相对路径、本地链接、M0–M12 编号、修改白名单与历史内容比对通过；29 个本地链接可解析，20 个代码块闭合。历史比对按 Git 的 LF/CRLF 规则规范化后，设计基线及 M0–M6 八份文件与 HEAD 内容一致。
- 加入本记录并同步执行计划后再次静态复核：五份交付文档、35 个本地链接、20 个代码块通过，历史文件保持原样，除本记录外仅原用户任务书为未跟踪文件。
- 同一静态脚本通过已安装 TypeScript 对架构中的八段类型定义在内存中合并检查，零诊断；不生成源文件，不代表应用实现或运行验收通过。
- git diff 全文及交叉引用人工审阅完成；git diff --check 通过。既有来源、裁图、设置及异常交互保留，修改仅在本阶段授权文档。

## 未执行项、Gate 与下一步

- 未修改 src、prompts、package.json、lockfile、tests、构建或工作流；未安装依赖、创建业务目录、调用真实模型或运行应用 build/browser/PWA 检查。它们不是 M7 文档 Gate，本记录不声称 v0.4 功能可运行。
- 原设计基线和 M0–M6 工作记录未改写；临时重构任务书保持未跟踪，不纳入提交或长期索引。没有新增其他报告或后续空记录。
- M7 文档 Gate 通过。提交、合并和远端结果以实际 Git 操作为准，不预填提交号或发布结果；本轮只按现有流程推送 main，不改变 Pages 配置。
- 完成 M7 后停止。M8–M12 均待实施；下一次授权可开始 M8 的行为保持模块化与工程整理，不在本轮提前实施。
