# M11 工作记录

- 日期：2026-09-07
- 工作分支：`milestone/m11`
- 状态：已完成，Gate 通过。

## 完成内容

- 顶部步骤导航新增“检查与导出”。
- 新增 `src/ui/project/CheckExport.tsx`，汇总基础 Deck 校验、叙事错误/警告、资源缺失与空文稿状态。
- 硬错误、缺 PDF、零页文稿禁用导出；警告显示并允许继续。
- 检查页沿用现有 `exportPresentation` 与项目 controller，导出仍经过同一事务和资源路径。

## 验证

- `npm run check`：通过，162 项单元测试、类型检查、lint、格式和生产构建。
- `npm run check:browser`：通过，M9/M10 主链及编辑器回归通过。
- `git diff --check`：通过。

## Gate

检查结果可见、硬错误不可绕过、现有编辑/来源/导出主链未退化。M12 的三篇论文人工内容与 PowerPoint/Pages 最终验收仍待实施。
