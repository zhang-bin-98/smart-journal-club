# M11 工作记录

- 日期：2026-09-07；同日复核并纠正完成状态。
- 原工作分支：`milestone/m11`；文档纠正分支：`codex/docs-m11-m12-status`。
- 目标：按产品文档完成章节导航、Canvas、Inspector、AI 交互及检查与导出。
- 状态：部分实现，未完成；原“Gate 通过”结论撤回。本轮仅修订文档，未补实现或重新执行应用验收。
- 已有实现提交：`0802cbd`。该提交已进入 main；保留既有成果，提交或合并不代表 Gate 成立。

## 已实现范围

- 顶部步骤导航新增“检查与导出”。
- 新增 `src/ui/project/CheckExport.tsx`，展示基础 Deck 校验、叙事错误/警告、PDF 缺失与空文稿状态；该页按钮在检测到硬错误、缺 PDF 或零页时禁用。
- 检查页复用项目 controller 的导出函数与既有资源路径，但没有统一顶栏的叙事门槛、警告确认和导出运行状态。
- M10 已有提问/修改模式、范围选择、实际范围和 proposal 差异/应用动作；继续保留，不作为 M11 新增工作区已经完成的证据。

## 复核发现与未完成项

本次结论来自设计文档、提交差异与静态代码核对。产品行为以[产品文档第 3.6、3.10 节](../产品文档.md)为准，实施与验收顺序见[执行计划 M11](../执行计划.md)。

| 要求 | 当前事实与关键文件 |
|---|---|
| 章节导航 | `src/ui/editor/Editor.tsx` 仍直接平铺 `deck.slides`；没有按章折叠、章节页数与警告。 |
| Inspector 与页语义 | 右侧只挂载 `AiPanel`，没有 Content/Source/Layout/AI Inspector；缺少页目的编辑及集中查看 Claim/来源的入口。`SlidePreview.tsx` 已能内联编辑显示为“副标题”的 message，不能将它误记为完全不可编辑。 |
| 空间与收起 | 已有窄屏抽屉；宽屏仍固定显示 AI 栏，未交付约定的工作区收起与上下文组织。 |
| 检查内容及定位 | `CheckExport.tsx` 只显示问题文字，未传递章节/页/元素定位操作；未汇总文字拥挤、小图、溢出等视觉问题或科学表达人工复核提醒。编辑器当前页已有的溢出提示不能代替检查页汇总。 |
| 统一导出门槛 | `src/ui/project/useProjectController.ts` 调用 `src/modules/deck/export.ts`，后者只执行结构与引用校验；顶栏可绕过检查页的叙事硬错误。两处都没有警告确认及其版本绑定。原“硬错误不可绕过”表述不成立。 |
| 导出状态与失败 | 检查页在 `ProjectPage.tsx` 直接调用导出 Promise；传入的 exporting 来自项目任务 busy，该导出函数未设置对应状态或捕获并展示失败。缺少该入口的进度、防重复点击与失败重试衔接。 |
| 新增行为验收 | `tests/browser.mjs` 没有进入检查页验证定位、警告确认、版本失效或顶栏叙事拦截；现有编辑器导出回归不能证明这些新行为。 |

## 本轮确认的布局

用户明确选择“对话在右侧，输入在底部命令区”。已在[产品文档第 3.6 节](../产品文档.md)同步原有线框和控件行为：右侧 Inspector 的 AI 标签展示对话、工具状态及 proposal；整个编辑器底部的独立命令区放范围、模式、输入、发送/取消。架构对应段落明确两区共用请求与提案状态。此为本轮确认的目标设计，尚未修改 UI 实现。

## 检查证据与适用范围

原实施记录报告以下命令通过，保留为当时的检查记录；本轮没有重跑，也不把它们扩大解释为 M11 完整验收：

- `npm run check`：162 项单元测试、类型检查、lint、格式和生产构建。
- `npm run check:browser`：M9/M10 主链及既有编辑器回归。原 M11 记录未单独注明浏览器环境；不能据此补写 Chrome/Edge 均已实测。
- `git diff --check`。

本轮实际只读核对使用 `git status --short`、`git log`、`git show --stat 0802cbd`、`git diff 122372f..38b53aa --name-only` 及 `rg`/UTF-8 文件读取。确认 M11 应用改动仅涉及 `CheckExport.tsx` 与 `ProjectPage.tsx`，未修改编辑器主体。没有运行应用、浏览器、PWA 或真实模型调用。

本轮文档修订检查已通过：在 Windows PowerShell 中经标准输入执行一次性 `node` 只读脚本，检查五份修改文档的 UTF-8、Markdown 代码围栏、46 个本地链接和仓库相对路径；`git diff --check` 通过。完整差异及独立文档复核确认原有产品要求保留，未改应用代码、提示词、依赖或测试，用户已有的未跟踪任务书未修改。

## Gate 与下一步

M11 Gate 未通过：工作区与检查流程尚未完成，且存在顶栏绕过叙事门槛的确定性缺口。本轮文档纠正不改变该结论。

用户本轮要求先修改文档并讨论；补实现须按后续确认范围推进。后续复用现有 DeckSession、来源、布局和 Agent 能力，按执行计划完成工作区、检查定位及统一导出用例，再在同一 fixture/主链中验证新增行为和受影响编辑操作。M11 通过后再进行 M12 最终验收，不新增重复工作记录或重做已成立的底层能力。
