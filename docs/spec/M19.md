# M19：整体验收

- 日期：2026-09-12。
- 工作分支：`milestone/m19`，从已同步的 `main`（`69e5836`）创建；开始时工作区干净，`main...origin/main` 为 `0 0`。
- 目标：按[当前阶段](../execution/当前阶段.md)复核四步主链、三篇科学内容、兼容、离线与实际发布，完成分层及旧路径清理。
- 状态：已完成，Gate 通过。用户已授权实施、清理及使用既有模型配置处理两篇公开 PDF；实现、固定回归、三篇科学验收及实际新版本 Pages 均通过。应用提交 `93060d5` 已快进合并到 main 并推送；本记录与执行状态在部署后归档。

## 已实施

### 分层和旧路径清理

- 领域收敛为 `project`、`paper`、`presentation`。旧 Deck 的薄重导出、旧 outline/generation/assistant 领域目录、五阶段 workflow、旧 Repository 提交、旧编辑器及开发 fixture 页面已退出源码；正式入口始终使用 `ProjectWorkspace`。
- `src/modules/presentation/editing/migrateDeck.ts` 保留旧稿兼容；`src/modules/presentation/legacy/` 只保存历史 Schema、叙事校验与纯 Build 合同，支撑原有纯 fixture 和兼容验证，不包含模型请求、存储或可执行的第二条生成链。
- IndexedDB 和历史记录实现归 `src/infrastructure/persistence/`。旧消息数据形状保留在 `legacyMessage.ts`，Pi 流适配归 `src/infrastructure/llm/assistantStream.ts`。图像预览归 `src/ui/presentation/SlidePreview.tsx`。
- project 的迁移从 paper 拆出；paper 来源规则不再引用 PDF 资源适配器。Prompt 文件加载归 infrastructure，应用工作流显式接收 `PromptCatalog`，UI 只读已装配的策略目录；后续实际验收仅补充已保存语言的传递要求，不改叙事策略。
- speech/slides/settings 存储及 Responses 适配器通过装配接收应用提供的纯校验。基础设施对计划/配置契约只作类型引用；公共模型错误类为明确保留的运行时错误边界。幻灯片服务的会话创建归 `app/presentation/service.ts`，composition 负责装配。
- `tests/unit/moduleBoundaries.test.ts` 从阶段白名单扩展到全部领域、应用工作流及 UI，检查三个领域、存储访问、模型入口、基础设施运行时依赖和静态运行时导入环。

### 复核发现的修复

- 旧稿在正式工作台跨章节移动时，插入锚点必须属于目标章节；新增 `moveSlideBy` 复用移动规则，旧稿正确换章，新稿保留讲述段落归属。拖放同步使用对应稿件版本的章节规则。
- 幻灯片 AI 发送时复制页面选择，避免调用方随后改变数组影响候选范围；一次请求只接受一份提案，后续工具调用不能替换已记录提案。
- 项目迁移先校验所有绑定稿件的可读版本，再写项目及论文，损坏/未来版本失败保持原记录。浏览旧稿使用兼容投影，不编造讲稿、递增文稿版本或删除旧临时计划。

- 真实 ModT 讲稿触发单批输出上限及冗余片段登记错误。发现批次上限从 32 调为 16，保留全部发现、证据与全文；生成入口根据片段明确的 paragraphId 补齐段落的有序 segmentIds，不改正文/引用/归属，重复身份或未知段落仍拒绝。普通编辑校验不放宽。
- 实际 ModT 出现英文讲稿、中文大纲与混合语言页面。organizeSpeech 与 planSlides 显式传递已保存的 content.language，相关阶段 Prompt 要求沿用；不以系统提示或章节标题语言翻译。固定检查覆盖每批与修复请求的语言传递及正文不变，真实重规划已验证。
- 清除产品、UI、架构和验证文档页头过时的“应用尚未实施”，统一引用执行目录的实际交付状态；不改变设计范围或历史阶段结论。

### 验证迁移

- `tests/browser.mjs` 连接 M15 正式分析主链和 `tests/m19-chain.mjs`，同一个项目完成图源确认、讲稿生成与人工编辑、页面生成、刷新和 PPTX 导出。
- 旧目标解析测试改验 UI 显式发送范围；保留空/失效选择、局部越界、只读无写工具、讲稿/插入锚点授权、候选原子性、版本/草稿漂移、失败不推进 Undo。旧自然语言页码推断与旧 Figure 专用权限不再是当前页面能力契约。
- `tests/ai-contracts.ts`、`contracts.ts`、`migration-contracts.ts`、`m15-persistence-contracts.ts`、`speech-storage-contracts.ts` 均改接正式存储/会话；真实 IndexedDB 覆盖预览不写、历史失败回滚、整批应用/Undo、重复应用拒绝、旧稿与旧计划保留、删除和裁图隔离。
- `tests/legacy-editor.mjs` 改在正式四步工作台打开原有历史 fixture，保留中文输入/焦点、页选择、跨章移动/Undo、删除图像、导出、刷新、空稿与新增页检查。
- 删除只调用退休五阶段链的 analysis/reanalysis/outline 合约和旧专用 UI 测试；当前单元/浏览器保留暂停、迟到响应、有限修复、手动续跑和候选保护。未减少三篇科学样例的 Gate。

## 已执行检查

环境沿用当前安装依赖：Node 24.19、TypeScript 5.9、Vitest 5、Biome 2.5、Vite 7、Edge headless。浏览器常规回归使用固定响应；以下另列必要的真实模型调用。

| 检查 | 结果与证据 |
|---|---|
| 修改前 `npm run check` | 通过，33 文件 / 273 单元测试；`output/playwright/m19-baseline-check.log`，仅为基线 |
| 当前 `npm run check` | lint、格式、30 文件 / 237 项单元测试、类型、Prompt 检查与构建通过；lint 保留现有风格提示，日志 `output/playwright/m19-release-check.log` |
| `node tests/browser.mjs` | 通过同项目四步主链、用户主动下一步、部分保存/刷新续跑、固定 PPTX 导出及当前 AI/编辑/迁移/Worker 合约；`output/playwright/m19-chain.log`、`m19-main-chain.pptx`、`m19-four-step-chain.png` |
| `node tests/m16-browser.mjs` | 图源固定交互通过；`output/playwright/m19-figures.log` |
| `node tests/m17-browser.mjs` | 讲稿交互和真实存储通过；`output/playwright/m19-speech.log` |
| `node tests/m18-browser.mjs` | 图组、分页、AI、导出、候选及上一版通过；测试补充 Redo 后等待图间距恢复的条件，未修改对应产品行为；`output/playwright/m19-slides.log` |
| `node tests/legacy-editor.mjs` | 迁到正式 UI 后通过；`output/playwright/m19-legacy-final.log` |
| `node tests/pwa.mjs` | 生产子路径、首次缓存失败/重试、冷启动离线 PDF/裁图/保存/延迟导出、更新等待/脏数据/其他标签保护和 API 不缓存通过；`output/playwright/m19-pwa.log` |

生产构建仍有原有大包体积提示。语言传递修改后的最终全套检查通过，固定主链通过；实际新版本发布验收也已通过。

## 三篇真实科学验收

沿用现有 PLOS 公开原创研究 PDF，不新增论文、模型或浏览器组合。三篇均只有主 PDF；其中补充图的图注/方法属于可提取全文，单独下载附件里的图像未包含在本次材料中，不声称已经展示那些补充图。

### 调用与失败记录

- 原自动审批拒绝的是两篇公开 PDF 向现有模型服务的发送；用户随后明确授权。之后实际使用已验证的 DeepSeek Responses 配置，未把 Key 写入仓库、记录、模型输出文件或产物。
- `tests/m19-live.mjs` 对机制和临床各完成全文分析。初版监视器误读另一个 Vite 模块实例的 idle 状态，在整体汇总尚未保存时关闭浏览器；修正后从已保存完整页单元手动续跑整体汇总，未重跑完整页分析。临床初轮观测到 44 个已结束响应，但旧记录文件被覆盖；机制初轮保留 56 个响应。不能据此给出完整账单/总调用数，关闭时的在途请求不确定。最终续跑时段分别为临床 277620 ms、机制 559303 ms，不当作全流程总耗时。
- 临床讲稿 6 批加 1 次组织，共 7 个实际请求，440682 ms；18 章页面规划 18 次请求，205781 ms。该轮无格式重试。
- 机制讲稿首轮 32 项/批，第 2 批达到 24576 输出 Token 上限而失败；16 项/批第二轮第 5 批的段落片段登记错误，经 1 次模型修复仍失败。两轮分别保留 2、6 个响应文件，未提交半份讲稿。增加确定性登记归一化后的第三轮实际发送 13 批及 1 次组织，共 14 次，806186 ms，完整保存。监视脚本曾打印复用 5 批的意图，但导航重新装配后封装未生效；实际响应时间戳证明 14 次均为新调用，记录不写成成功复用，并已删除未生效的恢复封装。
- 机制首次页面规划 23 次，451077 ms，生成 168 页；因中英混用保留为自动失败样例。另有一次检查脚本在人工修订前误启动页面规划，发现后中止，未形成新稿；在途消耗不确定。语言修复后实际组织 1 次、页面规划 19 次及其中 3 次有限修复，共 23 次，435040 ms；19 章、163 页全部英文，保存稿与原讲稿正文一致。该轮导出亦触发复用 Edge 配置退出，改由同一已保存稿在隔离浏览器导出；没有重新生成讲稿。
- 调用、已保存分析、自动稿、人工修改及截图保存在被忽略的 `output/playwright/m19-*`。有响应计数不等于成功内容计数；普通回归不使用这些真实请求。

### 全文、原句和引用

`node tests/m19-source-audit.mjs <三份最终快照>` 检查文件/页身份、Claim–Evidence–Source、精确文本切片、讲稿及页面引用、图块/Panel 归属和逐段唯一分配。三篇 98 页均有正文块；组学 485 块、机制 361 块、临床 558 块，原句定位分别为 1591、1248、1127 个，无已保存 quote 与原切片不一致。338/206/167 个已整理发现均有讲述，未记录省略。这些数字只说明契约和定位，不能证明模型找到了论文中的全部科学发现。

另以提取全文、全部 Claim 和讲稿核对研究问题、方法、主要发现、局限、补充证据；按原 PDF 逐一查看全部主图及原始/修订边界，读页面文字并在实际 PowerPoint 检查主图。发现列表存在摘要/正文重复、方法和参考资料被纳入发现的问题；稿件完整但偏长，不能宣称已自动得到适合短时现场汇报的精简成稿。

### 临床 VRC07-523LS

- 自动分析保存 25 页、167 个发现及全部 7 个主 Figure；选中图页 8 页，额外页无 Figure，不是漏主图。
- 实际检查原始及修订的 7 页：8 次最终边框修改、1 次临时扩框、23 次 Panel 删除。Fig 1B 恢复时间轴并排除 A 表格；Fig 2 恢复标题、右侧完整图例；Fig 3/4/5/6/7 合并需共享坐标/图例的孤立 Panel，保留 4 个独立 Panel 和全部 7 个 Figure。未将人工结果计为自动切分改善。
- 讲稿初稿 18 章、95 段、133 片段，人工修改 13 处：明确 T3 表头 IM 与设计 IV 的原文冲突、PK 分析排除范围、访视时点、浓度与滴度、Fig 5 轴方向、Fig 7 图例人数、Fig 6 首剂后 8 周、发表时研究状态、I 期研究不证明预防效力和原文 AUC 公式只转述未重算。
- 核对 0/16/32/48/64 周给药、112 周采样与 144 周安全随访；正文半衰期 42.43 天及 CI 与摘要舍入值分开，SC/IM 生物利用度为 0.40/0.57；PT80=200 为模型假设，首剂后 8 周最高剂量组仅 3/7 病毒株达标，不能当临床保护效力。既往 AMP 的敏感病毒结果不归给本研究。
- 页面原稿 95 页（72 主线、23 补充），分页后 140 片段、19 图像实例。人工改 6 页（11/29/30/38/52/94），纠正给药结束时间、接受度 >87% 的适用指标、模型相关性表述与临床推论强度。
- 最终真实 PowerPoint：95 页备注全部一致，285 个原生文字对象、19 张独立图片，比例一致、无文字溢出，改标题后保存重开通过。全部主 Figure 样例及修改页另查截图。复用的 Edge 临床配置在下载时进程退出；脱离应用、about:blank 下载 14 字节文本也复现，故定位为该浏览器配置环境问题。同一已保存稿、原图与讲稿在隔离浏览器中导出成功，模型调用为 0，原项目保留。未宣称已修复该浏览器配置。

### 组学 TORC1

- 复用 M15 全文分析、M17 真实讲稿和 M18 已保存稿，不再调用模型。M18 绑定仅继承 Fig 3 的 M16 手动边界及当时两页编辑，不能写成所有 Figure 都继承了 M16 修正。
- 原始稿与当前导出的来源、讲稿、页面逐字段一致后，实际查看 7 个主 Figure 原页和标框图；发现多处自动边界漏实验标签、内参、坐标轴或共享图例。通过正式 DeckSession 修改 53 页的 71 个 cropOverride 图像实例，保留工作 Paper/旧绑定与唯一讲稿；6 个 Fig 3 实例沿用原边界。共享坐标的 F–I 组及共享标题的 F/G 组保留完整组，不裁掉科学标注。
- 第 63 页同时在讲稿与页面注明 Fig 4A 图注 135 分钟、图内时间轴 150 分钟的原文差异；不默默决定哪个正确。其余正文沿用已核对的 M17/M18 科学措辞及限制，未重新生成。
- 最终 185 页（132 主线、53 补充），208 段、272 片段、77 图像实例；真实 PowerPoint 185 页备注全部一致，555 个原生文字对象、77 张独立图片，比例一致、无文字溢出，文字修改保存重开通过。全部主图样例已查，密集整图须放大阅读，不以缩小字体冒充远距离可读。

### 机制 ModT

- 保存 32 页、206 个发现及全部 6 个主 Figure；选中 9 页，额外页未生出串图。原始与修订的 6 个主图页均实际查看。
- 人工最终改框 18 次，另有 6 次临时扩框，删除 12 个无法独立承载坐标/图例的 Panel，保留 14 个 Panel 和全部 Figure。Fig 1 恢复 Northern 内参/标签；Fig 2/3 保留共用图例；Fig 4 恢复字母和比例尺；Fig 5/6 共享图例以整图承载，独立 Panel 保留完整轴/内参。
- 讲稿保存 154 段，首次页面规划分为 380 片段。人工修改 9 处，明确长异构体正文约 260 nt/图 258 nt、两个异构体与 242-nt marker 的位置、c-di-GMP 对产孢与毒素的不同解释、短异构体仅足以恢复生长、5 天观察的边界、原文 Fig 1B/C 与 Fig 4A/5B 错引、S1A 复用的历史 GEO 编号及按菌株解释产孢图。
- 已逐项核对 ModT(L)>180 min、ModT(S)=45.94 min、nc008=22.95 min；145/344 个差异基因、62/248 σB、18/40 σH、75/90 tRNA；长异构体恢复产孢、短异构体超产孢；双敲除恢复产孢但毒素仅部分恢复。远缘同源物挽救程度不同；蛋白结合、E-loop 必需性、配体/核酶均保留推测或未验证边界。
- 最终人工修订涉及 8 页：6/12/21/82/102/109 的表述或备注，以及 26/29 的 A–C 结构模型裁图；4 个讲稿片段同步区分长/短半衰期、碳源条件与原文分类疑点。A. arabaticum 被论文列为 Actinomycetota，但 [NCBI Taxonomy 28187](https://www.ncbi.nlm.nih.gov/Taxonomy/Browser/wwwtax.cgi?id=28187&mode=Info) 列为 Bacillota；备注明确这是 2026-09-12 的外部人工核对，不将其冒充论文证据，也不改变被测试同源物身份。
- 最终 163 页（117 主线、46 补充）、154 段、380 片段、49 图像实例；真实 PowerPoint 163 页备注全部一致，485 个原生文字对象、49 张独立图片，比例一致、无文字溢出，文字编辑保存重开通过。6 个主 Figure 样例、原始及最终结构比较裁图已实际查看，未将中英混用初稿记为通过。

## 验收入口与证据

- `tests/m19-live.mjs` 为两篇缺少 v0.5 全文证据的公开样例提供 UI 分析/保存/手动续跑；`tests/m19-review.mjs` 为图源快照、讲稿、页面及导出分段记录。
- `tests/m19-clinical-manual.mjs`、`m19-mechanism-manual.mjs` 使用正式图源会话保存修框；`m19-*-speech-manual.mjs`、`m19-*-pages-manual.mjs` 及 `m19-mechanism-focus.mjs` 记录针对已保存样例的人工修改。
- `tests/m19-omics.mjs`、`m19-omics-crops.mjs` 复用既有真实稿和原 PDF，并通过正式 DeckSession 修订。`tests/m19-language.mjs` 只重新组织和规划已保存讲稿，`m19-isolated-export.mjs` 验证复用稿与隔离导出之间科学内容逐字段不变。
- `node tests/m19-source-audit.mjs output/playwright/m19-omics-final.json output/playwright/m19-clinical-vrc07-phase1-trial-edited.json output/playwright/m19-mechanism-modt-cdifficile-edited.json`：三篇定位/引用/唯一分配检查通过，输出 `m19-source-audit.json`。
- `tests/m18-powerpoint.ps1 -DeckPath <最终 PPTX> -SnapshotPath <对应最终快照> -OutputPrefix <样例检查前缀> -AdditionalSlides <人工修改页>`：真实 PowerPoint 三篇共 443 页检查通过；该脚本现在遇到文字溢出也返回失败，支持显式追加修改页截图。分别见 `m19-omics-final-powerpoint.json`、`m19-clinical-final-powerpoint.json`、`m19-mechanism-final-powerpoint.json`；完整命令实际参数由对应导出文件名定位，不在仓库提交生成产物。
- `node tests/m19-pages.mjs`：既有已部署站点（版本 `e4d7fcb0d10e9ebc`）在线、模型配置返回、Service Worker 激活、离线重开通过；`m19-pages-baseline.json`。这是发布前基线，不能替代本次推送后的版本检查。

## Gate 与收尾

Gate 通过：目标四层与三个领域归位、旧调用链清理、同一四步主链、三篇科学内容与实际 PowerPoint、旧稿/候选/离线/更新、实际 Pages 已完成必要检查。最终 `npm run check`（30 文件、237 项测试）和 `node tests/browser.mjs` 通过；21 份变更 Markdown 本地链接及 git diff --check 通过，运行证据见上表及 `m19-release-check.log`、`m19-release-chain.log`。

应用与验收记录提交 `93060d5`（清理旧流程并完成 M19 本地整体验收），从 milestone/m19 快进合并到 main，仅推 main。推送前已重新 fetch，主分支未发生并行推进；没有覆盖用户改动、强推或改变发布配置。

[Pages 部署运行 34700097990](https://github.com/zhang-bin-98/smart-journal-club/actions/runs/34700097990) 对应该应用提交，结果 success。随后以 `SMARTJC_VERIFY_LOCAL_BUILD=1` 运行 `node tests/m19-pages.mjs`，实际站点在线入口、模型配置返回、Service Worker 激活、刷新与离线重开通过，页面错误与失败资源均为空；部署版本 `339f83ab9bff2b07` 与本地验收构建完全一致。结果见 `m19-pages-final.json`、`m19-pages-final.log` 和截图。此次完成状态回填只改文档，不改变该应用构建。

保留的限制：三篇生成均需要人工科学核对和修框，自动稿偏长且有重复；单主 PDF 未包含外部补充图像；密集整图需要放大阅读；复用的 Edge 验收配置发生与应用无关的下载退出，隔离配置已成功导出，不声称修复用户浏览器环境。没有 Keynote 验证，不改变部署配置或模型选型。M19 到此结束，不自动开始新里程碑。


## 后续用户要求：设置中清除本应用存储（2026-09-12）

用户在 M19 完成后提出增加清除应用全部存储功能，本次作为已发布版本的相关维护，使用 `codex/clear-app-storage` 分支，不开始新里程碑。

- 设置右栏增加本地数据管理入口，任务、保存和连接检查期间禁用。独立确认页要求输入“清除所有数据”，进入前说明丢弃未保存配置；原项目会话随页面导航卸载。执行期间不允许返回，成功后用户联网重新打开。
- `src/app/settings/resetStorage.ts` 协调运行状态、重入及失败语义；`src/infrastructure/persistence/storageReset.ts` 在单次事务清空七个 store，保留空 Schema，并清除应用命名空间 Web Storage、本部署 Service Worker 与离线 CacheStorage。其他应用数据库、缓存、存储和本机下载文件不删除。
- `src/main.tsx` 在应用启动前取得共享 Web Lock，清理使用独占锁，其他现存窗口阻断清理，新窗口在清理期间等待。切换独占锁前明确等待本页共享锁完全释放，避免误判当前窗口。Service Worker 同时复核旧页面，准备离线资源或更新时拒绝清理，清理期间停止缓存读写。
- `src/ui/settings/StorageResetPage.tsx` 只呈现确认/进度/结果，不挂载旧项目或模型任务。跨存储清理不存在全局事务；数据库失败整批回滚，缓存等后续失败明确提示可能部分删除并允许重试，不恢复旧内存快照。
- 产品、UI 和存储契约已在各自权威文档同步。没有改模型、提示词、科研稿件或部署配置。

实际验证：

- `npm run check`：31 个测试文件、239 项测试通过；lint、格式、类型与构建通过，保留已有大构建块警告。日志 `output/playwright/storage-reset-check.log`。
- `node tests/storage-reset.mjs`：生产子路径下的取消与输入确认、离线资源准备互斥、普通/旧标签页阻断、清理中新窗口等待、事务失败整批回滚、缓存失败部分清理与重试、离线执行、七个 store 全空、配置/Key 与缓存清理、同源其他数据保留、重新打开空状态通过。使用临时隔离 Edge 上下文和固定测试数据，未操作用户项目或调用模型；日志及确认/成功截图在 `output/playwright/storage-reset-*`，确认页已检查可读性。
- `node tests/model-settings.mjs`：原配置保存、清 Key、能力检查、失败、取消、忙碌和离线回归通过，增加运行中清理入口禁用断言；日志 `output/playwright/storage-reset-settings.log`。
- `node tests/browser.mjs`：同一四步主链、刷新、PPTX 和当前存储/AI 固定契约通过；日志 `output/playwright/storage-reset-chain.log`。
- `node tests/pwa.mjs`：首次缓存失败重试、生产子路径、离线 PDF/裁图/编辑/导出、旧缓存与更新保护、多标签页及无 API 缓存回归通过；日志 `output/playwright/storage-reset-pwa.log`。
- `git diff --check` 与本次 Markdown 本地链接检查通过。

限制：清理不可撤销；不支持 Web Locks 的浏览器拒绝此操作。清理后离线缓存已移除，必须联网重新打开并重新准备。浏览器存储元数据和浏览器自行管理的 HTTP 缓存不作为应用数据清理；下载到电脑的文件不受影响。遇到旧版离线服务不认识清理指令时须先更新应用。

## 发布后顶栏入口维护

用户要求安装与更新不再单独占用一行，并补充 GitHub 页面链接。本次维护将离线准备、安装、更新及失败重试收进首页、四步工作台和模型配置页的现有顶栏；更新仍沿用空闲、未保存输入和多标签页保护。顶栏同时提供在新标签页打开的 GitHub 项目链接，不修改 PWA 更新、存储或发布契约。

- 日期与分支：2026-09-12，codex/header-app-actions。安装、更新和重试使用带 tooltip、键盘焦点及可访问名称的紧凑图标按钮；GitHub 入口保留文字并指向项目仓库。
- npm run check：最终通过；lint 与 format 通过，31 个测试文件、239 项单元测试通过，类型、Prompt 和生产构建通过。保留既有 lint 提示与大构建块警告，本次未扩大范围处理。
- npm run check:browser：使用本机既有 Playwright 1.63 与 Edge headless，通过四步主链、编辑、迁移、刷新和 PPTX 导出；首次未启动约定的 5175 本地服务而连接失败，补启同一服务后通过。
- npm run check:pwa：生产子路径、首次缓存失败重试、冷启动离线重开、更新等待、未保存/运行中/多标签保护、数据保留与 API 不缓存通过；新增断言确认 GitHub 地址及新标签打开、重试/更新位于 header，并确认 1100px 更新态顶栏无横向溢出。最初采用文字按钮时该宽度断言失败，收紧为图标按钮后通过。
- Codex 内置浏览器的本地可视预览两次因界面辅助进程重置而未完成；未将其记为人工截图验收。实际 Edge 浏览器的主链、PWA 和 DOM 几何断言均已通过。
- 应用提交 [63650fa](https://github.com/zhang-bin-98/smart-journal-club/commit/63650fa192a66f4374e4c77e0180b838f3408944) 已快进合并并仅推送 main；[Pages 部署运行 34703625678](https://github.com/zhang-bin-98/smart-journal-club/actions/runs/34703625678) 完成且结果为 success。
- 以本地已验证构建运行 node tests/m19-pages.mjs，实际站点在线入口、模型配置返回、Service Worker 激活、刷新和离线重开通过；部署版本 111b7c66d43f5b74 与本地构建一致。

## 发布后论文页级截断修复

- 日期与分支：2026-09-19，`codex/fix-analysis-truncation`。用户反馈全文提取已完成、图源页分析报“模型输出未完成”；作为已发布版本维护处理，不启动新里程碑。
- 定位：`src/infrastructure/llm/responses.ts` 将模型结束原因 `length` 转为 `truncated`；`src/app/workflows/preparePaper.ts` 原先直接结束页级单元，未利用适配器已有的更大输出预算。截图和错误路径支持输出截断的判断，未取得该次供应商原始用量，不能判断是可见输出还是思考 Token 耗尽预算。
- 修改：仅页级首次截断将输出预算从 16384 提高至 24576 并重试一次；与格式/引用修复共用一次机会，普通格式修复不提高预算。取消、认证、网络等错误不新增重试。全文汇总仍由既有 24576 预算与细分批次逻辑处理。成果仍在完整单元通过校验后提交；不更改 Schema、Prompt、模型配置或 UI。
- `npx vitest run tests/unit/paperWorkflow.test.ts`：12 项通过。沿用双文件 fixture，验证扩容后完成、连续截断仅两次、失败定位文件/页、保留完整文本和兄弟单元、手动续跑跳过已保存单元、取消后不再请求及普通模型错误不重试；已有格式修复测试验证预算保持 16384。
- `npm run check`：31 个文件、243 项测试通过，lint、格式、类型、Prompt 校验及生产构建通过；保留既有 4 个 lint 警告和构建注释/大块提示。日志为 `output/playwright/analysis-truncation-check.log`。
- 验证边界：此次为工作流错误恢复修复，使用固定响应，不重跑三篇科学样例、浏览器全链或真实模型，不宣称用户当前论文已分析成功。实际论文仍可能在更大预算下截断；已保存内容保留，更新后手动继续未完成部分。
