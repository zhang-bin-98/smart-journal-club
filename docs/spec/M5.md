# M5 AI 修改与事务

- 日期：2026-09-06
- 工作分支：`milestone/m5`
- 状态：已完成；Gate 通过
- 目标：问答只读，AI 修改绑定明确目标，整批原子保存并支持一次撤销。

## 完成内容

- 延续已有基础提交 `b65244a` 和 AI 面板草稿，完成 `src/ai.ts`、`prompts/stages/ai.md` 与编辑器接入。请求先区分问答和修改，问答不开放写工具；按需读取当前论文概要、页文本、Figure、Claim 和 Deck。局部上下文只包含目标、必要相邻页及证据，叙事请求才带入策略。
- 应用在开始时绑定 Deck ID、revision、页码和元素目标；明确页码优先于当前选择。加入章节不存在、重复 Figure、空文稿和多轮指代处理，无法唯一定位时提示补充目标。标题请求从已有 Schema 派生仅允许目标页标题的工具参数。
- 唯一写工具 `deck.apply_revision` 只暂存一个合法候选，模型回合正常结束后通过原有 `DeckSession` 提交。取消、失败、重复调用或版本过期均不留下半修改。
- `src/storage.ts` 在同一 IndexedDB 事务保存 Deck、版本、修改记录和可见对话，保留最近 100 条可见消息；问答仅追加历史。保存失败、任务失效及事务中取消均回滚，Undo 仍递增版本。
- 编辑器先保存草稿再发送；文字或裁图首次产生内容草稿即取消旧 AI，浏览和选择不取消。摘要显示受影响页，只有仍为当前可撤销修改时允许直接撤销。补齐响应式 AI/导航抽屉、输入保留和焦点恢复。
- DeepSeek 不接受带点号的函数名，`src/model.ts` 仅在线上传输时映射本轮工具名，返回后还原内部逻辑名称；不增加供应商平台或代理。约束依据为 [DeepSeek Chat Completions](https://api-docs.deepseek.com/api/create-chat-completion/)。
- 修复基础提交将 Slide Schema 改严格后暴露的计划投影问题：`src/layout.ts` 构造校验用 Slide 时移除计划专属 `figures` 字段，保持既有生成主链可用。

## 实际检查

- `npm run build`：通过；5 个策略、5 个阶段提示词、TypeScript 与 Vite 构建通过。保留依赖既有 Zod Rollup 注释 warning。
- `npm run check:browser`：通过。复用原 Paper/Deck fixture、论文和主链，新增 `tests/ai-contracts.ts` 验证显式目标、Figure 歧义、问答权限、工具结果、多页一次 Undo、过期/重复请求、候选后失败/取消、事务中失效、摘要写入失败回滚及历史上限/删除。主链补入 AI 发送、草稿取消、摘要撤销和刷新恢复；固定 SSE 响应不消耗模型额度。
- Edge 独立响应式检查：桌面、平板和手机抽屉、关闭后输入保留、Escape 焦点恢复、选页后关闭导航、扩大窗口恢复滚动及无横向溢出通过，截图已检查。
- 真实 Edge / DeepSeek：复用 M4 组学项目的 16 页 Deck。只读解释保持 revision=0；当前查看第 3 页时明确修改第 1 页标题，只有目标标题改变；一次 Undo 恢复全部内容，revision=2。修改回合三个请求均为 HTTP 200，未重新分析论文。
- 真实标题修改最初两次因模型返回 element scope 配合 update-slide 被拒，原内容未改变；收紧本轮派生工具 Schema 后同一请求复验通过。失败原因及保护边界已由固定响应补充检查。
- `git diff --check`：通过。未安装 Chrome，浏览器验证使用 Edge，未声称 Chrome 实测。

## Gate 与下一步

通过。明确目标、局部范围、问答只读、批量原子提交、草稿取消及一次 Undo 已有固定和必要真实运行证据。M6 的整套重生成、上一版恢复、PWA 离线/更新及最终 Pages 发布验收尚未完成，继续复用三篇代表论文及同一主链。
