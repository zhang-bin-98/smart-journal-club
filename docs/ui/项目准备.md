# 首页与项目准备

> v0.5 目标设计，应用尚未实施。当前进入项目管理与新建准备的讨论；下文为已有文字基线，管理页布局、上传入口和准备表单的承载方式尚未确认，不代表已有 HTML。当前状态见 [执行目录](../execution/README.md)。

### 3.2 首页与项目准备

```text
┌────────────────────────────────────────────────────┐
│ smartJC                                  [模型设置] │
│                                                    │
│         上传论文，生成可继续修改的 PPT              │
│     ┌────────────────────────────────────────┐     │
│     │       将一篇论文 PDF 拖到这里           │     │
│     │              [选择 PDF]                │     │
│     └────────────────────────────────────────┘     │
│                                                    │
│ 最近项目                                           │
│  项目 A           15 页 · 已完成 · 2 小时前    [⋯]  │
│  项目 B           待继续 · 昨天               [⋯]  │
└────────────────────────────────────────────────────┘
```

- 上传区支持拖入和文件选择，只接受单份 PDF。保存时原位显示“正在保存论文…”，完成后进入项目准备状态。
- 最近项目按最近修改时间排列，显示名称、页数（已有 Deck 时）、状态和时间。点击打开，更多菜单只提供重命名和删除。
- 没有项目时隐藏列表，保留上传说明与主要按钮；读取失败显示原因和“重新选择 PDF”。
- 项目与原 PDF 一起保存成功后进入准备状态；项目名称先取文件名，仅在用户尚未重命名时再用论文标题补充。项目名称与论文原标题分开保存。

项目准备使用工作台中央的简短表单，仅包含原 PDF 与可选汇报要求；开始后原位进入进度，不新增长页面。下图为中央区域示意，外围沿用[工作台外壳](../ui/工作台与公共交互.md)外壳：

```text
┌────────────────────────────────────────────────────┐
│ [返回首页]  项目名称                     [模型设置] │
│                                                    │
│ 已保存：paper.pdf                                  │
│ 你希望怎么汇报这篇论文？（可选）                    │
│ ┌────────────────────────────────────────────────┐ │
│ │ 例如：中文组会，重点讲结果与创新点              │ │
│ └────────────────────────────────────────────────┘ │
│ 汇报要求自动保存；通过顶栏“分析论文”开始            │
└────────────────────────────────────────────────────┘
```

准备阶段保留自然语言要求输入框，可填写语言、听众、时长、重点和篇幅。默认采用论文主要语言并充分覆盖发现；这些汇报要求只影响大纲、讲稿和 PPT 取舍，不改变全文底稿的提取范围。模型 Key 未配置时在生成按钮旁提示并提供设置入口，配置后回到原项目，保留已输入要求。
