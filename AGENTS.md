# smartJC 工作约定

本文件适用于整个仓库，规定协作习惯和日常工作流程。产品、UI、架构和执行内容分别维护在 docs/product、docs/ui、docs/architecture、docs/execution 中；实际里程碑工作记录保存在 `docs/spec/`，不复制产品与架构定义。

## 沟通与决策

- 使用中文沟通，先说明结果或判断，再补充必要依据；进展更新聚焦发现、下一步和实际阻塞。
- 按用户已确认的目标推进，常规、可逆的实现细节自行决定，不反复请求已经给出的授权。
- 对影响产品范围、关键交互、数据契约、部署方式或明显增加工程量的未决问题，先核对现有约定，再带着具体问题、影响和推荐方案讨论；等待时继续不依赖该决定的工作。
- 用户最新明确要求优先于仓库中的旧约定。已经授权的调整直接落实，并同步相关文档；不要把自己的假设写成用户已确认的决定。
- 清楚区分设计建议、已实现内容和实际验证结果；没有证据时标注未验证。

## 开始工作

1. 阅读当前任务、相关目录的工作约定，以及 [产品目录](docs/product/README.md)、[UI 规格](docs/ui/README.md)、[架构目录](docs/architecture/README.md)、[执行目录](docs/execution/README.md) 中相关部分；检查实际文件和 Git 状态。
2. 明确本次目标、所属阶段、涉及文件和必要验收。每个里程碑开始实施前，先按下文 Git 规则从 `main` 创建独立工作分支；继续同一里程碑时使用已有对应分支。涉及多个步骤时先简述实施顺序。
3. 以执行计划中的实际记录确定阶段，不把文档中的示例、候选技术或待实现路径当成现成代码。只推进当前已授权范围，完整处理其中必要的修改和验证。
4. 保留已有未提交改动；发现与本次工作直接冲突的改动时先厘清，不能覆盖用户成果。

## 实施与文档维护

- 保持 smartJC 短小、清楚、可维护。先复用现有实现和已选依赖，再写必要的薄封装；只为当前真实职责创建 feature module，不创建空抽象、空目录、占位接口或第二套流程。
- 界面统一使用 Tailwind CSS 工具类，并复用统一主题和控件样式；新增页面与状态不得另写一套组件 CSS。主题、全局样式与运行时几何的边界见 [分层与模块](docs/architecture/分层与模块.md)。
- 每次围绕一个具体问题修改，只做相关重构，避免顺手扩展功能、替换技术栈或大范围格式化。每个里程碑只处理一个主要目标；M7 仅文档，M8 只保持 v0.3 行为的模块化，不夹带 Schema、Prompt、生成质量或产品交互变化。M7 完成后停止，后续每轮只实施一个已授权里程碑。
- v0.5 的 M13 仅维护设计文档目录、用户授权的 HTML 原型、本文件及本阶段工作记录；完成后停止，不提前修改应用、Prompt、依赖或测试。M14 起按执行计划逐轮授权实施，M0–M12 的历史阶段规则不阻止已确认的新流程。
- 项目准备归入新建流程；一个项目包含一份主论文 PDF 和可选一份补充 PDF。按 documentId 区分文件内页码/原文/图源，两份材料共同解析，文件角色与汇报主线/补充独立。具体契约见 docs/architecture，不把双文件支持当成已实现。
- v0.5 以论文内容为基础完整整理发现与证据；四步为“论文分析 → 图源核对 → 大纲与演讲稿 → 幻灯片”。论文分析展示双文件进度和全部页面供图源页查漏；完成后必须用户点击进入图源核对，Figure 切分仍需整体确认；之后继续由用户主动进入下一步；检查融入编辑，导出前自动校验，不设独立检查步骤。步骤内部自动完成必要生成，每步允许编辑；不把预算、来源登记或模型格式修复转嫁给用户。新行为的实施状态以执行计划为准。
- 图源切分允许用户直接调整单页图块/Panel 边框、统一添加 Figure（选择已有图号追加图块、输入新图号创建新图）、补删 Panel 和改标签；标题可原位选择/输入图号，只改当前图块归属，归入已有 Figure 需确认；“整图边界”只调整已有图块，单图允许无 Panel；科学标注完整优先于无重叠，不抹白或重绘原图。重新识别先给候选，不覆盖人工结果；来源级修改保护旧稿和上一版的论文绑定，单页 cropOverride 仍独立。
- 分页保持大纲段落身份，讲稿唯一正文派生备注；页面更新建议不阻断导出，确定性引用/分配/备注错误与页面溢出须处理。
- 论文分析完整单元及时保存，刷新后手动续跑未完成部分。
- 幻灯片图组首选有限矩形分区与候选评分，保存页内图像实例、分区比例和间距，预览与导出共用几何；候选库仅按 docs/architecture 的验证边界评估，不把研究结论当已实现能力。
- M8 的模块化为当前实现基线；v0.5 目标采用 UI / Application / Domain / Infrastructure 四层和 project / paper / presentation 三个领域，按 [分层与模块](docs/architecture/分层与模块.md) 渐进归位。工作流与 Agent 接线归 app，外部适配器归 infrastructure；旧 outline/deck 的共同领域内容归 presentation，不提前生成空目录或第二套流程。
- 本项目桌面优先，不设计响应式断点、自动抽屉或手机布局；分隔线、焦点和内容可读性按桌面实际操作验收。
- UI 按 docs/ui 的页面规格重新设计完整应用工作台，不能仅将旧长网页套入固定高度。主操作、对象导航、切分工具、直接编辑、AI 收起、自动保存、键盘焦点与桌面分栏调节按实际任务验收；当前实现状态以执行计划为准。
- 应用用例统一协调状态转换和跨对象原子提交；领域规则与影响分析可无浏览器测试，生成流程可无 React/真实模型测试。UI/tool 不导入具体适配器，composition 只装配；Plan 与 Deck 复用共同定义和必要纯规则，保留各自会话与生命周期。
- 固定解析、图源、论文理解、大纲/演讲稿、下一步页面规划、按已保存且校验就绪的计划生成及导出使用显式 workflow；交互式问答与读工具选择在 M10 必须优先使用 `@earendil-works/pi-agent-core` 的 Agent、事件与取消能力，不维护第二套通用 Agent loop。M8 只隔离旧循环，替换留到 M10。
- UI 与 Agent tool 不直接访问 IndexedDB，不承载领域校验、事务或持久化；通过 service/use case 和 repository 接入。Schema、validator、mutation 不依赖 React、DOM、PDF.js、PptxGenJS 或 Pi；DeckSession/OutlineSession 保持单一提交入口。
- 稳定 Schema、基础叙事骨架、确定性校验和事务规则不能由 Prompt 替代；可变研究写作建议才放提示词。
- docs/product 负责范围与产品原则，docs/ui 负责页面与交互；docs/architecture 分别维护分层、数据模型、约束、接口和运行契约；docs/execution 负责阶段交付、当前状态和记录索引。docs/prototypes 保存已授权的交互 HTML，原型不充当应用实现。验证证据、实际改动和阻塞详情只在对应的 `docs/spec/` 工作记录维护。
- 合并或精简文档前核对原有独有信息，尤其保留页面线框、控件行为和异常状态；将必要内容移入权威位置，不能只删掉旧章节。
- 发现冲突时修改原有权威段落及受影响引用，不在文末追加另一套“最终方案”。每个里程碑维护一份工作记录，不另建重复任务索引、日报或总结文档。
- 研究叙事提示词按架构约定维护；调整叙事指导使用配置文件，稳定的数据契约、布局及提交规则留在实现中。
- 搜索优先使用 `rg`，集中读取相关文件。使用 UTF-8，修改后检查差异，避免无关文件和生成产物混入。
- 文档、工作记录和交付说明中的文件位置使用仓库相对路径；Markdown 文件之间的链接相对当前文件。不写入本机盘符、用户目录或机器专属的绝对路径；运行环境参数不复制到记录中。
- 依赖与服务能力先检查实际安装版本和官方资料，复用已验证结论；技术路线的通过与否以执行计划要求的真实运行证据为准。

## 可读性与工程质量

- 一行表达一个主要动作，不用分号压缩声明、分支和副作用；避免深层三元表达式和超长内联回调。
- 页面组件主要组合 UI；异步业务过程放明确的 service/use case/controller hook，不在 React render 或 effect 中动态 import 核心业务模块。
- 同一请求参数超过 5–6 个时改用具名对象；Query 不产生副作用，Command 名称明确表达写入。
- 错误统一带 stage、code、message、recovery，不在各处散落无法判定的错误字符串；UI 只展示必要产品信息，不暴露原始 provider 错误体、工具 JSON、Key 或隐藏推理。
- 公共函数为关键不变量、事务和失败语义写短 JSDoc，不注释显然语法；Agent 工具名称、参数 Schema、实现和 UI label 放同一职责模块。
- 不设机械全局 LOC 上限；一个文件同时承担三种以上职责，或已难以独立验证时拆分。跨模块通过公开入口导入，不建立环依赖或重型基础设施。
- M8 引入单一格式/lint 工具及 Vitest 或等价轻量测试，提供 format、lint、test、check 脚本；不叠加 ESLint、Prettier、Biome 三套重叠工具。必要一次性格式化独立提交，不混入逻辑迁移。
- 测试只保护迁移、全文证据/图源核对、讲稿与页面同步、计划版本与原子性、Agent 权限/proposal、DeckSession 和共享几何等实际风险，沿用同一 fixture、主链与三篇人工样例，不引入覆盖率目标或测试矩阵膨胀；常规回归使用固定响应。

## 验证与收尾

1. 先审阅差异，确认目标已满足，相关文档和实现一致，没有丢失已有设计或引入范围外功能。
2. 只修改文档时，检查术语、范围、接口、阶段顺序、本地链接及 Markdown 格式，并运行 `git diff --check`；不为文档变更新增应用代码或测试框架。
3. 修改代码时，从仓库实际脚本中选择必要的类型、构建和相关测试检查，完成当前阶段的验收；不虚构命令或把未运行写成通过。
4. 复用执行计划规定的同一组 fixture、主链和三篇人工样例，只补实际改动带来的风险检查。不增加覆盖率目标、重复端到端流程或模型/浏览器/论文类型组合矩阵；已通过的检查只在相关变化或失败需要时重跑。
5. 真实模型调用用于当前阶段必要验证，常规自动回归使用固定响应；不为重复检查消耗模型额度。API Key 不进入仓库、日志、提示词文件或部署产物。
6. 里程碑完成后，将实际工作情况写入 `docs/spec/M<编号>.md`，在执行计划同步状态、Gate 结论和记录链接；记录与实现一起提交，完成后按下文流程合并到 `main`。小型文档修改不另拆日报，可并入所属阶段记录。
7. 必要检查失败时先定位修复；环境或外部条件阻塞时如实说明已完成部分、未验证项和下一步，不降低验收标准来宣称完成。

## 里程碑工作记录

目录名固定为 `docs/spec/`。每个里程碑一份 Markdown 文件，如 `M0.md`、`M1.md`；完成时创建并随该阶段成果提交，不预建空记录。设计阶段基线使用 `设计基线.md`。记录只写实际工作情况：

- 里程碑、日期、工作分支、目标及完成状态。
- 完成内容、关键文件和必要的设计决定；范围变化引用已更新的权威文档。
- 实际执行的检查命令、环境、结果和 Gate 结论；未执行项及原因单独说明。
- 未解决问题、与计划的偏差和下一步；已有关键实现提交可附提交号。

必要验收受阻时可在同一文件记录真实进展并标注未完成，不能将该阶段标为通过或合并。执行计划只保留状态和链接，不重复粘贴整份记录；不在记录中预填尚未产生的提交号或合并结果。

## Git 工作方式

- 主分支为 `main`。每个里程碑实施前，从已核对并更新的本地 `main` 创建独立分支，命名为 `milestone/m0`、`milestone/m1` 等；不直接在 `main` 开展里程碑实现。已配置远程时先核对并同步主分支，不能强制覆盖分叉。
- 继续同一里程碑时复用其工作分支；同名分支存在时先检查内容，不能重置或覆盖。下一里程碑从上一阶段合并后的 `main` 创建，不串接未完成分支。
- 每个独立、有用且通过必要验证的改动，在工作分支上自动提交，无需逐次询问。一个提交表达一个完整意图，相关文档和实现一起提交。
- 提交前审阅完整差异，只暂存本次相关文件或改动块；不使用全量暂存把用户已有改动一并纳入。提交说明使用简洁中文，描述具体变化。
- 里程碑收尾时核对 `main` 是否前进；如有新提交，先合并到工作分支、解决冲突，并完成受影响的必要检查，再定稿工作记录。
- 验收通过且工作记录和执行计划状态已提交后，自动将该里程碑分支合并到 `main`；可以使用快速合并，优先尝试 `--ff-only`。只有主分支已前进、无法快进且仍需保留阶段合并节点时，才使用 `--no-ff`，无需再次询问。合并后检查提交关系和工作区状态，留在 `main`；工作分支默认保留。
- 必要检查未通过时继续修复或报告阻塞，不把半完成工作作为已验证成果提交或合并；不要为了让工作区干净而删除或重置已有修改。
- 用户已授权 agent 推送远端：必要验收通过、相关记录提交并合并到 `main` 后，自动且仅推送 `main` 到已配置远端，无需逐次询问。除 `main` 外的分支只保留在本地，不推送远端。推送前核对远端更新，禁止强制推送或改写已有提交历史；按现有工作流触发 Pages 部署，不擅自改变发布配置。

## 交付回复

简要说明完成内容、关键文件、实际检查结果和必要限制。自动提交后附提交号；如果仍有未提交改动或阻塞，准确说明。后续建议只列与当前目标直接相关的下一步。
